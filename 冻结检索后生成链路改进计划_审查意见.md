# 冻结检索后生成链路改进计划 —— 审查意见

审查日期：2026-09-19
审查对象：`冻结检索后的生成链路改进计划`
审查方式：只读核对代码、评测结果、脚本与配置

---

## 一、总体结论

**这份计划的诊断质量很高，六个问题里有五个是准确的，可以直接执行。**

与前一份计划相比，这一份的进步在于：不再猜测，而是基于实测数据定位。
"q09/q10 是服务超时而非证据不足"、"span recall 被字符串匹配误判"、
"citation validity 对 fallback 答案失效"这几条，我都逐一复核过，全部属实。

但有 **1 条诊断结论需要修正**（问题 3 的严重程度被低估），
**1 条漏掉了**（评测输入文件存在被误覆盖的隐患，计划提到了但没意识到它多危险），
以及 **Phase 3 的"单次请求、失败即记录"策略有一个副作用**需要提前处理。

逐条说明如下。

---

## 二、已核实的诊断（可直接执行）

### ✅ 问题 1：q09/q10 首次失败是服务超时

复核 `data/generation_eval_full.json`：

| 题 | 状态 | 延迟 | 错误 |
|---|---|---|---|
| q09_hit | generation_error | 121.764s | Request timed out. |
| q10_hit | generation_error | 122.337s | Request timed out. |

配置核对无误：

```text
config.py:87      llm_timeout_seconds = 60        （.env:34 覆盖）
src/model_clients.py:92   max_retries = 1
```

60 × (1 + 1) = 120s，与实测 121~122s 完全吻合。
重跑 `generation_eval_q09_retry.json`（15.0s）和
`generation_eval_q10_retry.json`（28.8s）均成功。
**结论成立：证据包充足，属上游瞬时超时。**

### ✅ 问题 2：span recall 误判属实

我逐条核对了 `data/generation_eval.json` 里的标注与
`frozen_evidence.py:_normalise_text` / `_span_matches` 的实现。
计划列举的四个案例全部属实，包括：

- `soft_audit` 的正则 `_CITATION_RE = re.compile(r"\[([LW]\d+)\]")`
  只做编号存在性检查，不做主张级覆盖。

`soft_audit` 里确实只算了 `citation_validity = not invalid`（第 364 行），
**没有任何"事实主张是否有引用"的逻辑**。计划问题 3 的判断准确。

### ✅ 问题 4：q10 回答过长

实测 `generation_eval_q10_retry.json`：`answer_length = 589`，
提示词第 5 条虽写了"控制在约 600 字以内"，但 589 已贴着上限，
且按你描述列出了几十个方法。**成立。**

### ✅ 问题 5：结果文件未统一

实测存在 8 个 `generation_eval*.json`：

```text
generation_eval.json                 ← 评测输入（cases）
generation_eval_chroma_offline.json
generation_eval_full.json            ← 含旧 q09/q10 超时结果
generation_eval_q01.json
generation_eval_q09_retry.json
generation_eval_q10_retry.json
generation_eval_results.json
generation_eval_smoke.json
```

`generation_eval_full.json` 里 q09/q10 仍是 `generation_error`，
确实不能代表最新状态。**成立。**

### ✅ 问题 6：既有测试失败属实

实跑确认：

```text
FAILED tests/test_graph.py::test_web_policy_blocks_graph_search_without_calling_service
E  assert any(item["status"] == "blocked" for item in result["execution_trace"] ...)
```

与计划描述一致，且属于旧图链路，不影响新生成实验。

---

## 三、需要修正的问题

### ⚠️ A. 误覆盖风险比计划描述的更严重（Phase 1 必须最先做）

计划说"`scripts/Run-GenerationReplay.ps1` 的默认输出路径与评测输入路径相同，
存在误覆盖 `generation_eval.json` 的风险"。

这不只是"风险"。实际核对脚本：

```powershell
[string]$EvaluationPath = "data\generation_eval.json",
[string]$OutputPath     = "data\generation_eval.json",   # ← 同一个文件
```

**默认参数下，跑一次脚本就会把 12 题的评测标注直接覆盖成结果文件。**
而 `generation_eval.json` 里的 `expected_answer_spans` 是人工积累的语义基准，
一旦覆盖，重跑评测就没有 ground truth 了——而且没有备份、没有版本控制（项目非 git 仓库）。

我确认过目前**尚未触发**（该文件仍含 `cases`，12 题），
但这是"还差一次默认调用"的事故，不是概率问题。

**建议：把它从 Phase 1 的清单里提出来，作为 Phase 0 的立即动作。**
在改任何指标之前，先做两件事：

1. 把 `$OutputPath` 默认值改为 `data\generation_eval_results.json`；
2. 备份 `data/generation_eval.json` 为
   `data/generation_eval.golden.json`，并在脚本里加一道断言：
   输出路径不得与输入路径相同，否则直接报错退出。

### ⚠️ B. 问题 3 的严重程度被低估：这不是"偏宽松"，而是"失效"

计划把 citation validity 描述为"目前偏宽松"。
但看实测数据，它的实际表现是**在失败案例上给出满分**：

```text
q09_hit  status=generation_error
         answer="当前生成模型不可用，无法生成正式回答；冻结证据已保留，请稍后重试。"
         used_citations = []
         citation_validity = True      ← 一条引用都没用，却判为有效
```

同一份结果里，`source_coverage = 0.0`、`span_recall = 0.0`，
但 `citation_validity = True`。这意味着**验收目标里"citation validity 100%"
在当前实现下是不具信息量的**——它无法区分"回答引用全对"和"根本没回答"。

根因在 `soft_audit` 第 328 行：

```python
invalid = sorted(set(used) - allowed, ...)
...
"citation_validity": not invalid,
```

空集合的差集是空集，所以 `not []` → `True`。
**只要不引用，就永远不会失效。**

这与你在 Phase 2 里列出的 `citation_validity` 目标直接冲突。
建议改为三态：

```text
citation_validity = "pass" | "fail" | "not_applicable"
```

其中 `not_applicable` 用于 fallback / 拒答 / 空答案，
不参与分子分母统计。否则 Phase 4 的"citation validity 100%"
会被大量 fallback 答案稀释成假通过。

### ⚠️ C. Phase 3 的"单次请求、失败即记录"需配套改动

计划提出评测模式默认单次请求、不自动重试，这个方向对——
避免把失败藏在重试里。但有个副作用：

当前 `src/generator_v2.py` 的 `generate_from_pack` 在异常时
会走 `_fallback_answer`，把 error 吞掉后仍返回结果。
如果把 `max_retries` 降到 0、同时保留这个 fallback，
**超时会被记录成 `status=generation_error` 但答案是一句套话**，
后续 span/source 指标全部为 0——像 q09/q10 那样。

这本身没错，但必须保证 Phase 2 新增的
`generation_success` 指标能**优先于**其他指标判读。
建议在 `GenerationResult` 里显式区分：

```text
generation_status ∈ {ok, provider_timeout, provider_error, empty_answer}
```

而不是只看 `error is not None`。
同时把 `request_attempt` / `retry_count` / `provider_error`
从 `settings` 侧改到**每次请求的返回值**里记录——
目前 `max_retries` 写死在 `src/model_clients.py:92`，
无法按评测/生产模式分别设置，需要在 `chat_model()` 加参数透传。

### ⚠️ D. 密钥一节的表述需要修正

计划说"`.env` 中出现了明文 API 密钥。如果该密钥仍然有效，应立即轮换"。

`.env` 在 `.gitignore` 里（已核对），属常规做法，明文存储本身不是问题。
我用真实密钥前缀做了全盘扫描（`logs/**`、`data/generation_eval*.json`、
所有 `*.md`、`.workbuddy-ai/**`）：

```text
真实密钥出现位置: 0
```

**密钥没有泄漏到任何日志、结果或文档。**
所以这不是"已发生的事故"，而是"防泄漏的预防要求"。
建议改为：保持 `.env` 不进版本库；新增的结果文件里不得写入
`LLM_API_KEY` / `LANGCHAIN_API_KEY`（Phase 1 增加元数据时容易顺手带上）。

---

## 四、两个可以简化的地方

### 1. Phase 1 的合并流程可以更简单

计划要"新增统一结果合并流程"来处理 q09/q10 的 retry 文件。
但如果 Phase 3 已经决定"单次请求、失败即记录"，
那 retry 文件本身就是评测模式的例外产物，不值得为它建一套合并器。

更简单：**每次评测输出一个带时间戳的完整文件**
（如 `data/gen_runs/20260919_1930.json`），
需要对照时读两个文件即可。避免"合并"这种会产生新状态的操作。

### 2. Phase 4 的验收目标需要加上区分度检查

目标是"所有失败都能区分为模型、服务、提示词、证据包或评测器问题"。
建议在 Phase 1 的元数据里就固化一个字段：

```text
failure_attribution ∈ {model, provider, prompt, evidence_pack, evaluator, unknown}
```

出问题时人工/规则填一次即可。
否则到了 Phase 4，你手上只有"失败"这个状态，还是无法归因。

---

## 五、明确认可的部分

- **冻结边界的刻画准确。** 计划列的禁改清单（`retriever.py`、`chunking.py`、
  `embeddings.py`、`reranker.py`、`ingest.py`、Chroma、`RETRIEVER_*`、
  `final_R.json`）与实际代码结构吻合，`frozen_evidence.py` 确实做到了
  不 import 任何检索模块。
- **Phase 顺序合理。** 先修评测可信度、再改生成、最后接回应用，
  这个顺序避免了"用不可信的指标指导优化"。
- **"复杂集只做回归、不用于调参"是对的。** 这是防止过拟合评测集的关键约定。

---

## 六、建议的执行顺序（微调后）

```text
Phase 0   修脚本误覆盖 + 备份 golden 标注 + 冻结输出路径断言   ← 立即做
Phase 1   统一结果与实验元数据（含 failure_attribution）
Phase 2   重做评测契约（citation_validity 改三态）
Phase 3   生成稳定性（generation_status 细分 + 重试参数化）
Phase 4   重建基线
Phase 5   接回应用
```

把原 Phase 1 里的"修脚本"提到最前面，
因为它是唯一一个**再跑一次就会造成不可逆损失**的问题。

---

## 七、一句话总结

诊断准确、方向正确，可以执行；但请先把 `Run-GenerationReplay.ps1`
的输出路径冲突修掉（现在跑一次默认参数就会覆盖人工标注的评测基准），
并把 `citation_validity` 从"不引用即通过"改成能识别 fallback 的三态指标——
否则 Phase 4 的两条验收目标（citation validity 100%、失败可归因）
在当前实现下都无法真正验收。
