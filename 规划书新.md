# 设计知识库助手 - 生产级完善路线图（状态校准版）

> 文档说明：下方四能力路线保留为历史规划，部分待办描述已经被当前代码实现。作品集 v1.0 的实际交付边界和执行步骤见 `作品集第一优先级交付计划.md`。本文件中的“待完成”不能直接视为当前代码缺失。

## 2026-09-13 当前状态

| 能力 | 当前真实状态 | 证据 |
|---|---|---|
| 文档入库与 R 分块 | 已实现 | `ingest.py`、`src/chunking.py` |
| 混合检索与重排 | 已实现并完成10题基线 | `src/retriever.py`、`src/reranker.py`、`logs/retrieval_ablations/final_R.json` |
| 任务拆解与并行子任务 | 已实现 | `src/planning.py`、`src/services.py`、`src/graph_builder.py` |
| 工具注册、超时与重试 | 已实现 | `src/tools/__init__.py`、`src/tools/builtin.py` |
| 熔断、并发与 token 配额 | 已实现 | `src/resilience.py`、`app.py` |
| 检查点与证据审计 | 已实现基础能力 | `src/checkpoint.py`、`src/evidence.py` |
| 单次执行追踪 | 已实现 | `src/graph_nodes.py`、`app.py` |
| 作品集可复现交付 | 本轮整理中 | `作品集第一优先级交付计划.md` |
| 生产级多用户监控/回放 | 尚未完成 | 不属于当前 v1.0 交付范围 |

R 策略10题当前组合基线为：Source hit mean 96.67%、Recall@10 96.67%、MRR 93.33%、NDCG@10 84.32%、重排成功率100%，平均检索延迟约26秒。它证明的是来源召回能力，不是最终答案正确率。

## 历史路线（归档参考）

---

## 📊 原规划与现状对比（历史记录）

### ✅ 已完成（MVP阶段）

| 功能模块 | 完成度 | 说明 |
|---------|--------|------|
| 文档入库 | 100% | 支持 PDF/TXT/MD，GPU 批量嵌入，增量更新 |
| 本地检索 | 100% | Chroma + BGE-M3，MMR 去重 |
| CRAG 评分 | 100% | LLM 逐一评分文档相关性 |
| 网络搜索 | 100% | DuckDuckGo 回退机制 |
| LangGraph 流程 | 100% | retrieve → grade → decide → web_search/generate |
| Gradio 界面 | 100% | 流式输出，来源展示，路径可视化 |
| 基础评测 | 80% | 有 eval_langsmith.py 和 20 问测试集 |
| 错误处理 | 70% | 节点级 try-catch，状态传递错误信息 |

### 🔨 原规划中曾列出的增强项（不代表当前缺失）

| 能力维度 | 当前状态 | 生产级要求 |
|---------|---------|-----------|
| **任务拆解** | 单轮问答，无多步规划 | 支持复杂查询拆解、子问题并行、结果聚合 |
| **工具调用** | 仅检索+搜索，无权限/上下文管理 | 工具注册机制、调用链追踪、异常重试、超时控制 |
| **可观测性** | 仅日志输出，无结构化追踪 | 全链路追踪、LangSmith 深度集成、性能指标、回放能力 |
| **生产鲁棒性** | 基础异常捕获 | 熔断降级、中断恢复、人工审核点、成本监控、并发控制 |

---

## 🎯 四大能力提升路线

## 能力一：任务拆解能力（Agent Planning）

### 现状问题
- 只能处理单轮简单问题（"办公椅座高是多少？"）
- 无法拆解复杂任务（"对比三种材料的优劣并推荐最适合户外家具的"）
- 无子问题并行能力

### 目标
- 判断问题复杂度，自动拆解为子任务
- 并行执行独立子任务
- 聚合多个结果生成最终答案
- 记录决策过程和依赖关系

### 实施步骤

#### 1.1 增加问题复杂度判断节点
```python
# src/graph_nodes.py 新增
async def analyze_question(self, state: GraphState) -> GraphState:
    """判断问题是否需要拆解"""
    question = state["question"]
    
    # 使用 LLM 判断复杂度和子任务
    analysis = await self.services.analyze_complexity(question)
    
    return {
        "question_type": analysis["type"],  # simple / complex / comparison
        "sub_questions": analysis.get("sub_questions", []),
        "requires_parallel": analysis.get("parallel", False),
        "route": "plan" if analysis["type"] != "simple" else "retrieve"
    }
```

#### 1.2 实现子任务并行执行
```python
# src/graph_builder.py 修改
workflow.add_node("analyze_question", nodes.analyze_question)
workflow.add_node("plan_execution", nodes.execute_plan)
workflow.add_node("aggregate_results", nodes.aggregate)

workflow.set_entry_point("analyze_question")
workflow.add_conditional_edges(
    "analyze_question",
    lambda s: "plan" if s["question_type"] == "complex" else "retrieve",
    {"plan": "plan_execution", "retrieve": "retrieve"}
)
```

#### 1.3 新增服务接口
```python
# src/services.py 扩展
class Services(Protocol):
    async def analyze_complexity(self, question: str) -> dict: ...
    async def execute_subquestions(self, questions: list[str]) -> list[dict]: ...
    async def aggregate_answers(self, sub_results: list[dict], original_q: str) -> str: ...
```

#### 1.4 验收标准
- [ ] 能识别对比类问题（"ABS vs PC 哪个更适合？"）
- [ ] 能拆解为 2-4 个子问题
- [ ] 子问题可并行检索
- [ ] 最终答案整合所有子结果
- [ ] 在状态中记录完整的任务树

---

## 能力二：工具调用能力（Tool Orchestration）

### 现状问题
- 工具硬编码在节点中（retriever、web_search）
- 无统一的工具注册和调用机制
- 缺少权限控制和调用审计
- 无工具调用超时和重试

### 目标
- 动态工具注册机制（未来可扩展计算器、数据库查询等）
- 统一的工具调用接口，带参数验证
- 调用链追踪（哪个节点调用了哪些工具）
- 异常处理和降级策略

### 实施步骤

#### 2.1 建立工具注册系统
```python
# src/tools/__init__.py (新建)
from dataclasses import dataclass
from typing import Any, Callable

@dataclass
class Tool:
    name: str
    description: str
    func: Callable
    parameters: dict  # JSON Schema
    timeout_seconds: float = 30.0
    max_retries: int = 2
    requires_approval: bool = False

class ToolRegistry:
    def __init__(self):
        self._tools: dict[str, Tool] = {}
    
    def register(self, tool: Tool):
        self._tools[tool.name] = tool
    
    async def call(self, name: str, **kwargs) -> dict:
        """带追踪、超时、重试的统一调用"""
        tool = self._tools[name]
        # 实现调用逻辑、日志、监控
        ...
```

#### 2.2 将现有功能改造为工具
```python
# src/tools/builtin.py (新建)
from src.tools import Tool, ToolRegistry

def register_builtin_tools(registry: ToolRegistry):
    registry.register(Tool(
        name="local_retrieval",
        description="从本地设计知识库检索相关文档",
        func=retrieve_documents,
        parameters={
            "type": "object",
            "properties": {
                "query": {"type": "string"},
                "top_k": {"type": "integer", "default": 5}
            }
        }
    ))
    
    registry.register(Tool(
        name="web_search",
        description="使用 DuckDuckGo 搜索网络资料",
        func=web_search,
        parameters={...},
        timeout_seconds=15.0
    ))
```

#### 2.3 在节点中调用工具
```python
# src/graph_nodes.py 改造
async def retrieve(self, state: GraphState) -> GraphState:
    tool_call_id = str(uuid.uuid4())
    try:
        result = await self.services.tool_registry.call(
            "local_retrieval",
            query=state["question"],
            _call_id=tool_call_id  # 追踪 ID
        )
        documents = result["documents"]
        tool_calls = [{"id": tool_call_id, "tool": "local_retrieval", "status": "success"}]
    except TimeoutError:
        documents = []
        tool_calls = [{"id": tool_call_id, "tool": "local_retrieval", "status": "timeout"}]
    
    return {
        "documents": documents,
        "tool_calls": state.get("tool_calls", []) + tool_calls
    }
```

#### 2.4 验收标准
- [ ] 所有工具通过 ToolRegistry 注册
- [ ] 工具调用有超时控制（15s 检索，30s 生成）
- [ ] 失败后自动重试（最多 2 次）
- [ ] 状态中记录完整的工具调用链
- [ ] 支持工具调用审计日志

---

## 能力三：评测与可观测性（Observability）

### 现状问题
- 已有 LangSmith 评测框架，但未充分利用
- 缺少性能指标（延迟、token 消耗）
- 无法方便地回放和调试历史会话
- 评测仅在开发时手动运行

### 目标
- 全链路追踪（每个节点的输入输出、耗时）
- 生产环境自动采样并上传到 LangSmith
- 性能监控 Dashboard
- 回放和对比功能

### 实施步骤

#### 3.1 深度集成 LangSmith Tracing
```python
# config.py 扩展
@dataclass(frozen=True)
class Settings:
    # ... 现有配置
    langsmith_api_key: str = os.getenv("LANGCHAIN_API_KEY", "")
    langsmith_project: str = os.getenv("LANGCHAIN_PROJECT", "design-kb-prod")
    langsmith_sampling_rate: float = float(os.getenv("LANGSMITH_SAMPLING_RATE", "0.1"))
    enable_tracing: bool = os.getenv("LANGCHAIN_TRACING_V2", "false").lower() == "true"
```

#### 3.2 节点级性能监控
```python
# src/graph_nodes.py 增强
import time
from src.monitoring import record_node_metrics

async def retrieve(self, state: GraphState) -> GraphState:
    start_time = time.time()
    try:
        documents = await self.services.retrieve(state["question"])
        status = "success"
    except Exception as exc:
        documents = []
        status = "error"
    finally:
        duration = time.time() - start_time
        record_node_metrics(
            node="retrieve",
            duration=duration,
            status=status,
            doc_count=len(documents)
        )
    ...
```

#### 3.3 构建监控指标收集器
```python
# src/monitoring.py (新建)
from dataclasses import dataclass, field
from typing import Optional

@dataclass
class NodeMetrics:
    node_name: str
    duration_seconds: float
    status: str  # success / error / timeout
    doc_count: Optional[int] = None
    token_count: Optional[int] = None

class MetricsCollector:
    def __init__(self):
        self.metrics: list[NodeMetrics] = []
    
    def record(self, **kwargs):
        self.metrics.append(NodeMetrics(**kwargs))
    
    def summary(self) -> dict:
        return {
            "total_duration": sum(m.duration_seconds for m in self.metrics),
            "node_breakdown": {
                m.node_name: m.duration_seconds 
                for m in self.metrics
            },
            "total_tokens": sum(
                m.token_count for m in self.metrics 
                if m.token_count
            )
        }
```

#### 3.4 Gradio 界面增加性能面板
```python
# app.py 扩展
with gr.Accordion("性能指标", open=False):
    metrics_output = gr.JSON(label="本次查询性能")

# 在 chat 函数中
final_metrics = metrics_collector.summary()
yield (answer, route, sources, errors, final_metrics)
```

#### 3.5 自动化评测流水线
```bash
# scripts/run_nightly_eval.sh (新建)
#!/bin/bash
set -e

echo "=== 夜间自动评测 ==="
source .venv/bin/activate

# 1. 检索预检
python eval_langsmith.py --check

# 2. 完整评测
python eval_langsmith.py --dataset design-knowledge-qa-20

# 3. 生成报告
python scripts/generate_eval_report.py --output logs/eval_$(date +%Y%m%d).md

echo "=== 评测完成，报告已保存 ==="
```

#### 3.6 验收标准
- [ ] 每次查询自动上传追踪到 LangSmith（采样率可配置）
- [ ] 节点级性能指标可视化（哪个节点最慢）
- [ ] token 消耗统计（评分、生成各用多少）
- [ ] 支持通过 run_id 回放历史查询
- [ ] 自动化夜间评测并生成趋势报告

---

## 能力四：生产环境能力（Production Readiness）

### 现状问题
- 无并发控制（多用户同时查询可能 OOM）
- 无熔断机制（LLM API 挂了整个系统不可用）
- 缺少人工审核点（敏感问题直接回答）
- 无成本控制（token 消耗无上限）
- 中断后无法恢复（用户刷新页面一切重来）

### 目标
- 系统可在生产环境 7×24 稳定运行
- 部分组件故障时优雅降级
- 支持中断恢复（保存中间状态）
- 成本可控（设置 token 上限、评分批量化）
- 关键操作支持人工审核

### 实施步骤

#### 4.1 实现熔断器模式
```python
# src/circuit_breaker.py (新建)
import time
from enum import Enum

class CircuitState(Enum):
    CLOSED = "closed"     # 正常
    OPEN = "open"         # 熔断
    HALF_OPEN = "half_open"  # 试探恢复

class CircuitBreaker:
    def __init__(self, failure_threshold=5, timeout=60):
        self.failure_threshold = failure_threshold
        self.timeout = timeout
        self.failures = 0
        self.state = CircuitState.CLOSED
        self.last_failure_time = None
    
    async def call(self, func, *args, **kwargs):
        if self.state == CircuitState.OPEN:
            if time.time() - self.last_failure_time > self.timeout:
                self.state = CircuitState.HALF_OPEN
            else:
                raise Exception("Circuit breaker is OPEN")
        
        try:
            result = await func(*args, **kwargs)
            if self.state == CircuitState.HALF_OPEN:
                self.state = CircuitState.CLOSED
                self.failures = 0
            return result
        except Exception as e:
            self.failures += 1
            self.last_failure_time = time.time()
            if self.failures >= self.failure_threshold:
                self.state = CircuitState.OPEN
            raise e
```

#### 4.2 服务降级策略
```python
# src/services.py 增强
class ResilientServices:
    def __init__(self):
        self.llm_breaker = CircuitBreaker(failure_threshold=3)
        self.web_breaker = CircuitBreaker(failure_threshold=5)
    
    async def grade(self, question: str, documents: list[Document]) -> list[Document]:
        try:
            return await self.llm_breaker.call(grade_documents, question, documents)
        except Exception:
            # 降级策略：改用基于关键词的简单评分
            print("[降级] LLM 评分不可用，使用关键词匹配")
            return keyword_based_filter(question, documents)
    
    async def search(self, question: str) -> list[Document]:
        try:
            return await self.web_breaker.call(web_search, question)
        except Exception:
            print("[降级] 网络搜索不可用，返回空结果")
            return []
```

#### 4.3 并发控制
```python
# app.py 增强
import asyncio

MAX_CONCURRENT_REQUESTS = 5
semaphore = asyncio.Semaphore(MAX_CONCURRENT_REQUESTS)

async def chat(message: str, history: list[dict[str, Any]]):
    async with semaphore:
        # 现有的查询逻辑
        ...
```

#### 4.4 成本监控与限制
```python
# src/cost_tracker.py (新建)
from dataclasses import dataclass
import threading

@dataclass
class CostLimit:
    daily_token_limit: int = 1_000_000
    per_query_token_limit: int = 10_000

class CostTracker:
    def __init__(self):
        self.daily_tokens = 0
        self.lock = threading.Lock()
        self.reset_time = time.time()
    
    def check_and_record(self, estimated_tokens: int) -> bool:
        with self.lock:
            # 每日重置
            if time.time() - self.reset_time > 86400:
                self.daily_tokens = 0
                self.reset_time = time.time()
            
            if self.daily_tokens + estimated_tokens > CostLimit.daily_token_limit:
                raise Exception("已达到每日 token 上限")
            
            self.daily_tokens += estimated_tokens
            return True
```

#### 4.5 中断恢复（检查点）
```python
# src/checkpoint.py (新建)
import json
from pathlib import Path

class CheckpointManager:
    def __init__(self, checkpoint_dir: Path):
        self.checkpoint_dir = checkpoint_dir
        self.checkpoint_dir.mkdir(parents=True, exist_ok=True)
    
    def save(self, session_id: str, state: GraphState):
        path = self.checkpoint_dir / f"{session_id}.json"
        with path.open("w", encoding="utf-8") as f:
            json.dump(state, f, ensure_ascii=False, default=str)
    
    def load(self, session_id: str) -> Optional[GraphState]:
        path = self.checkpoint_dir / f"{session_id}.json"
        if not path.exists():
            return None
        with path.open("r", encoding="utf-8") as f:
            return json.load(f)
    
    def resume_from_checkpoint(self, session_id: str):
        """从上次中断的地方继续"""
        state = self.load(session_id)
        if state:
            # 找到上次执行到哪个节点，从下一个节点继续
            ...
```

#### 4.6 人工审核点（可选）
```python
# src/graph_nodes.py 增加审核节点
async def require_human_approval(self, state: GraphState) -> GraphState:
    """敏感问题需要人工审核"""
    question = state["question"]
    
    # 判断是否需要审核（例如包含"删除"、"修改数据库"等关键词）
    if self.services.is_sensitive(question):
        # 将任务放入审核队列
        approval_id = await self.services.submit_for_approval(state)
        return {
            "status": "pending_approval",
            "approval_id": approval_id,
            "generation": "您的问题已提交人工审核，预计 10 分钟内回复"
        }
    
    return {"status": "approved"}
```

#### 4.7 验收标准
- [ ] LLM API 挂掉时，评分降级为关键词匹配，系统仍可用
- [ ] 并发请求超过 5 个时排队等待，而非直接失败
- [ ] 每日 token 消耗达到上限时拒绝新请求
- [ ] 用户中断查询后可通过 session_id 恢复
- [ ] 熔断器在连续 3 次失败后打开，60 秒后自动尝试恢复
- [ ] 敏感问题进入人工审核队列

---

## 📅 实施时间线（建议 4 周）

### Week 1: 任务拆解 + 工具调用基础
- [ ] Day 1-2: 实现 `analyze_question` 节点和复杂度判断
- [ ] Day 3-4: 建立 ToolRegistry 和工具注册机制
- [ ] Day 5: 集成到现有流程并测试

### Week 2: 工具调用完善 + 可观测性基础
- [ ] Day 1-2: 实现工具超时、重试、调用链追踪
- [ ] Day 3-4: LangSmith 深度集成，节点级性能监控
- [ ] Day 5: Gradio 界面增加性能面板

### Week 3: 生产环境能力
- [ ] Day 1-2: 熔断器、降级策略
- [ ] Day 3: 并发控制、成本监控
- [ ] Day 4: 检查点与中断恢复
- [ ] Day 5: 人工审核点（可选）

### Week 4: 评测完善 + 文档输出
- [ ] Day 1-2: 扩展测试集到 50 问（覆盖复杂查询、对比、多步骤）
- [ ] Day 3: 自动化评测流水线和报告生成
- [ ] Day 4: 性能压测（模拟 10 并发用户）
- [ ] Day 5: 完善文档和演示材料

---

## 🎬 Demo 场景设计（面试展示）

### 场景 1: 复杂任务拆解
**用户提问**："对比 ABS、PC、PP 三种塑料，哪种最适合户外椅子，给出理由。"

**系统行为**：
1. `analyze_question`：识别为对比类问题，拆解为 3 个子问题
   - "ABS 塑料的耐候性和机械性能"
   - "PC 塑料的耐候性和机械性能"
   - "PP 塑料的耐候性和机械性能"
2. 并行检索 3 个材料的资料
3. `aggregate_answers`：综合对比并给出推荐
4. 界面展示完整的任务树和子问题结果

**展示点**：任务拆解能力、并行执行、结果聚合

### 场景 2: 工具调用与降级
**模拟故障**：关闭 LLM API（或设置无效 key）

**用户提问**："办公椅座高标准是多少？"

**系统行为**：
1. `retrieve`：正常召回文档
2. `grade_documents`：LLM 评分失败 → **触发熔断器**
3. **降级**：改用关键词匹配（"办公椅"、"座高"）
4. `generate`：正常生成（如果评分降级成功仍有相关文档）
5. 界面显示："运行状态：部分能力降级（文档评分使用关键词匹配）"

**展示点**：熔断机制、降级策略、系统韧性

### 场景 3: 全链路追踪与回放
**操作流程**：
1. 提问："GB/T 10000 调查了哪些人体尺寸？"
2. 查询完成后，点击"查看详细追踪"按钮
3. 展示 LangSmith 追踪页面：
   - 节点执行时序图
   - 每个节点的输入输出
   - token 消耗分布（retrieve: 0, grade: 1500, generate: 2800）
   - 总耗时 3.2 秒
4. 选择历史某个 run_id，点击"回放"
5. 系统重新执行相同的查询路径，可对比结果差异

**展示点**：可观测性、调试能力、性能分析

### 场景 4: 成本控制与并发
**模拟高并发**：
1. 设置 `MAX_CONCURRENT_REQUESTS = 2`
2. 同时发起 5 个查询请求
3. 前 2 个立即执行，后 3 个显示"排队中（当前位置：1/2/3）"
4. 设置 `daily_token_limit = 50000`（仅用于演示）
5. 多次查询后触发上限
6. 新查询返回："系统今日 token 配额已用尽，请明日再试或联系管理员"

**展示点**：并发控制、成本监控、资源保护

---

## 📦 交付物清单

### 代码交付
- [ ] `src/planning/` 目录：任务拆解、子问题执行
- [ ] `src/tools/` 目录：工具注册、调用、追踪
- [ ] `src/monitoring.py`：性能指标收集
- [ ] `src/circuit_breaker.py`：熔断器实现
- [ ] `src/checkpoint.py`：中断恢复
- [ ] `src/cost_tracker.py`：成本监控
- [ ] `scripts/run_nightly_eval.sh`：自动化评测脚本
- [ ] 扩展的测试集（50 问）

### 文档交付
- [ ] 本路线图（`CRAG_PRODUCTION_ROADMAP.md`）
- [ ] 架构决策记录（ADR）：为什么选择熔断器、如何设计降级
- [ ] 运维手册：如何部署、监控、排查故障
- [ ] 演示脚本：4 个 demo 场景的操作步骤

### 评测数据
- [ ] 50 问测试集的评测报告（source_hit、answer_correctness）
- [ ] 性能基准测试报告（P50/P95 延迟、token 消耗）
- [ ] 压力测试报告（10 并发下的成功率、降级次数）

---

## 🎓 面试展示话术（3 分钟版）

**开场（30秒）**
> "这个项目是一个生产级的 AI Agent，核心是 CRAG + Web Search。我从 MVP 开始，用 20 个测试用例验证了基本功能。今天展示的是如何将它提升到生产环境标准，主要体现四个工程能力。"

**能力一展示（45秒）**
> "首先是任务拆解。看这个问题：'对比三种材料，哪种最适合？'系统会识别这是对比类问题，拆解为 3 个子问题并行检索，最后聚合结果。这里的关键是动态判断复杂度，而不是写死规则。"
> 
> [演示复杂查询界面，显示任务树]

**能力二展示（45秒）**
> "第二是工具调用。我建立了 ToolRegistry，所有能力都注册为工具——检索、搜索、未来还能扩展计算器、数据库查询。每个工具有超时控制、重试机制、调用链追踪。这里我关掉 API 模拟故障，看系统如何降级。"
> 
> [演示 API 故障时的降级行为]

**能力三展示（45秒）**
> "第三是可观测性。每次查询都追踪到 LangSmith，可以看到节点执行顺序、耗时、token 分布。这是刚才那次查询的追踪，评分用了 1500 token，生成用了 2800。还能回放历史查询进行对比。"
> 
> [打开 LangSmith 追踪页面]

**能力四展示（30秒）**
> "最后是生产鲁棒性。有熔断器（连续失败自动断开）、并发控制（排队而非拒绝）、成本监控（token 上限保护）。这些让系统可以 7×24 稳定运行。"

**结尾（15秒）**
> "完整的实现路线在这份文档里，包括 4 周时间线、50 问测试集、压测数据。我认为做 AI Agent 不只是调通流程，更重要的是工程化——让它可观测、可控制、可信赖。"

---

## 📊 成功指标（KPI）

### 功能指标
- [ ] 复杂查询拆解成功率 > 80%
- [ ] 工具调用成功率 > 95%（含降级）
- [ ] 答案正确率 > 85%（50 问测试集）

### 性能指标
- [ ] P95 响应时间 < 5 秒
- [ ] 检索延迟 < 500ms
- [ ] 生成延迟 < 3 秒

### 可靠性指标
- [ ] 服务可用率 > 99%（7 天测试）
- [ ] 降级后仍可用（正确率 > 70%）
- [ ] 熔断器恢复时间 < 60 秒

### 成本指标
- [ ] 平均每次查询 < 5000 tokens
- [ ] 评分优化（批量评分）节省 30% tokens

---

## 🔗 参考资料

- LangGraph 文档：https://langchain-ai.github.io/langgraph/
- LangSmith 追踪：https://docs.smith.langchain.com/
- 熔断器模式：Martin Fowler - Circuit Breaker
- 可观测性最佳实践：OpenTelemetry
- AI Agent 评测：LangChain Evaluation Framework

---

**文档版本**：v1.0  
**最后更新**：2026-08-19  
**负责人**：设计知识库助手项目组
