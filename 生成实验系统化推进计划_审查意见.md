# 生成实验系统化推进计划 —— 审查意见

审查日期：2026-09-19
审查对象：`下一步不要改检索器，也不要重建快照`（提示词实验矩阵方案）
审查方式：逐条复现核对基线结果、审计实现与提示词

---

## 一、总体结论

**这份计划是目前三版里最成熟的一版，方向完全正确，可以直接执行。**

它最大的进步是：不再试图"改进生成器"，而是把生成变成一个
**可比较、可解释、可复现的实验系统**。这正是这个项目从"能跑"走向
"能证明"的关键一步。

我复核了计划的每一条诊断，**没有发现错误的判断**。
基线文件的工程质量也超出预期——已经有 `run_id`、`snapshot_sha256`、
`evaluation_sha256`、`prompt_version`、分位数延迟、
`provider_timeout_rate` 等实验元数据，这正是 Opik/LangSmith 式可观测性。

下面只提 **3 处需要补充修正**，以及 **1 个执行顺序建议**。
另外有一个我在复核中发现的、计划尚未提到的问题，放在第三节。

---

## 二、已复核确认的诊断（无需改动）

基线文件 `data/runs/generation_replay_20260919T211836+0800_814cd403.json`
实测：

| 项 | 值 |
|---|---|
| total / completed | 12 / 12 |
| generation_success_rate | 1.0 |
| retrieval_calls | 0 |
| provider_timeout_rate | 0.0 |
| provider_error_rate | 0.0 |
| latency p50 / max | 3.661s / 9.523s |
| citation_status_counts | pass: 12 |
| refusal_correctness_rate | 1.0 |
| ambiguity_safety_rate | 1.0 |

**与计划描述完全一致。**

### ✅ q04 标注误判（已复现根因，并已实测出修复方案）

```text
标注期望：座深340mm~460mm
模型回答：座深（T1）：340 mm～460 mm
```

答案数值完全正确，但被判 `matched: false`。

根因不完全是计划说的"区间匹配误报"，而是**归一化后残留了标签字符**：

```text
exp_norm = "座深340~460mm"
ans_norm = "座深t1340~460mml1"
```

`（T1）` 的字母数字 `t1` 被保留下来，
**同时破坏了两个 token**：`座深` → `座深t`，`340` → `1340`。
所以 token-window 和 stable-tail 两条回退路径都无法命中。

**已实测验证的修复方案**（在原匹配器前加一道预清洗）：

```python
CIT = re.compile(r'\[[LWlw]\d+\]')
PAREN = re.compile(r'[（(][^）)]{1,12}[）)]')

def preclean(value):
    return PAREN.sub('', CIT.sub('', str(value or '')))
```

关键点是**顺序**：必须在 `_normalise_text` **之前**剥离，
否则括号内字符会先被字符过滤器捡回来。

实测效果（7 个案例）：

| expected | 原实现 | 修复后 | 结论 |
|---|---|---|---|
| 座深340mm~460mm | False | **True** | 修复 |
| 座前宽不小于400mm | True | True | 无变化 |
| 软面最大座高为460mm | True | True | 无变化 |
| 400mm~440mm | True | True | 无变化 |
| 4岁~17岁 | True | True | 无变化 |
| 顶层设计与系统化设计 | False | False | 标注问题，另处理 |
| 优化设计 | True | True | 无变化 |

**q04 修复，其余零破坏。**

⚠️ 注意：**不要**试图通过删掉 `≥`→`ge` 之类的符号转换来修，
也不要改成纯 substring 匹配。实测证明
`座前宽不小于400mm` 和 `软面最大座高为460mm` 现在能通过，
**正是依赖原有的 token-window / stable-tail 回退逻辑**。
动那些逻辑会弄坏现在正确的题。只加预清洗，不动匹配器本体。


### ✅ q09 标注过严（已复现）

```text
标注期望：顶层设计与系统化设计
模型回答：产品顶层设计"和"产品系统化设计
```

这是**语义等价但序列不连续**的典型情况。
模型把并列关系拆成了两个带修饰的短语，标注要求连续串。

计划判断正确。建议标注改为两个独立 span
（`顶层设计`、`系统化设计`），而不是放宽匹配算法——
因为放宽算法会引入误通过风险，改标注更安全。

### ✅ q12/q17 分离（已确认指标为空）

实测 `audit` 中：

```text
q12_miss:       refusal_correctness = true,  ambiguity_safety = null
q17_ambiguous:  refusal_correctness = null,  ambiguity_safety = true
```

**这两个字段已经实现了，且已按题型互斥填充。**
但 q12 的 `expected_answer_span_recall` 仍在计算（当前 0.0），
q17 也是（当前 0.333）。计划提出的"从普通 span 指标中分离出去"
正是要做的事——**数据结构已就绪，只需在汇总时不把它计入均值**。

### ✅ citation_coverage 名不副实（已确认）

实测 q01：`citation_coverage = 0.4`，而 `used_citations = [L1, L2]`、
`allowed_citations = [L1..L5]`。

`0.4 = 2/5` —— 确认它算的是"用了多少个允许编号"，
**不是"多少事实主张有引用"**。计划判断准确。

顺带一提：这个指标目前**越低越可疑但方向模糊**——
q01 用 2 个引用、cit_cov 0.4，q10 用 4 个、cit_cov 0.8。
按这个定义，**答案引用越多分越"高"**，与实际质量无关，
建议直接重命名为 `citation_id_usage_ratio`，另设真正的 claim-level 指标。

### ✅ q17 输出污染（已确认）

```text
... 具体计算方法和儿童尺寸范围，当前资料无法确认。.calc
```

末尾确实有孤立 `.calc`。计划判断准确。

### ✅ q10 过长（已确认）

`answer_length = 606`。计划判断准确。

---

## 三、计划未覆盖的一个问题（建议补入）

### ⚠️ `citation_status` 的"pass"含义仍然过窄

上一轮我提过 `citation_validity` 存在"不引用即通过"的缺陷。
现在看基线，已经改成了三态 `citation_status`，实测全 12 题都是 `pass`。

但 q12、q17 这两道**拒答/歧义题**也拿到了 `pass`：

```text
q12_miss:       citation_status = pass, used_citations = [L2,L3,L4,L5]
q17_ambiguous:  citation_status = pass, used_citations = [...]
```

拒答题拿到 `pass` 本身合理（它确实引用了证据来解释为什么无法确认），
但**它和事实题走的是同一个判定分支**。
到了 Phase 4 统计"citation_validity 100%"时，
拒答题的 `pass` 会混进事实题的分子，稀释指标的含义。

**建议：`citation_status` 增加第四个取值，
或增加独立的 `applicable_to_citation_metric` 布尔标记**，
让拒答/歧义题不参与 citation 类指标的统计。
这与计划第二节"把 q12、q17 从普通 span 指标中分离"是同一个逻辑，
但计划只提了 span，**没提 citation**。

### ⚠️ P2 结构化输出有一个隐藏风险

计划提议 P2 让模型输出 JSON（`answer` + `claims` + `uncertain_points`）。
方向对，但要注意：

`gpt-5.6` 通过 `StrOutputParser` 直接拿文本（见 `generator_v2.py:76`），
**没有 JSON 解析层**。如果 P2 直接让模型输出 JSON，
一旦模型返回带 markdown 代码围栏（```json ... ```）或截断的 JSON，
整题会走 fallback，看起来像"generation_error"，
**但真实原因是解析失败而非服务问题**。

建议 P2 实施时：
1. 结果里新增 `output_parse_status ∈ {ok, fenced, truncated, invalid}`
   与 `generation_status` **分开记录**；
2. 复用现有 `parse_relevant_ids` 里那种"正则提取 JSON 对象"的容错思路
   （`src/grader.py:77`），不要裸 `json.loads`。

否则 P0/P1/P2 的对比会被 P2 的解析失败污染，得出"结构化输出更差"的错误结论。

---

## 四、需要修正的两处细节

### 1. 验收线 `unsupported_claim_count = 0` 当前无实现

基线 audit 里有 `unsupported_claim_count` 字段，实测全为 0。
但请核实它的计算方式——当前 `soft_audit` 里只有
`unsupported_number_count`（数字级）和 `unsupported_numbers`（列表）。

**如果 `unsupported_claim_count` 只是数字级检查的别名，
那它无法支撑"claim-level faithfulness"这个目标。**
Phase 2 声称要做的 claim-level 审计，需要真正基于 P2 的 `claims` 数组
逐条判定，不能沿用数字匹配。

建议在 Phase 2 明确写出：`unsupported_claim_count` 的计算依赖
P2 的结构化输出；P0/P1 版本下该指标标记为 `not_applicable`。

### 2. "重复运行 3 次"需要固定 provider 侧变量

计划要求 P0/P1/P2 各跑 3 次。当前 `max_retries=1`
（`src/model_clients.py:92`）写死在代码里，
我在上一轮审查中提过它无法按模式配置。

**在进入 3×3 矩阵之前，建议先把重试参数化**，
否则 9 次运行里混着"重试成功"和"一次成功"两种语义，
P50/P95 延迟的对比会失真。

具体：给 `chat_model()` 加 `max_retries` 参数透传，
评测模式固定为 0（失败即记录），
并在结果里记录 `attempt_count`——基线文件里已经有这个字段了，
只需确保它真的被填充为 0/1。

---

## 五、建议的执行顺序（微调）

计划给的顺序：

```text
修正评测契约 → 输出清洗 → P0/P1/P2 实验 → 各跑3次 → 选版本 → 接回应用
```

建议调整为：

```text
0. 重试参数化（chat_model 加 max_retries 透传）
      ↓                ← 先做，否则后面 9 次运行的延迟不可比
1. 修正评测契约（含 span 归一化 bug + q12/q17 从 span/citation 双分离）
      ↓
2. 输出清洗层（raw/normalized 双留存 + sanitization_rules）
      ↓
3. 建立 P0 基线（用新契约重跑一次，作为对照锚点）
      ↓                ← 计划缺这一步
4. P1 / P2 提示词实验
      ↓
5. 每版本重复 3 次，统计 P50/P95
      ↓
6. 选稳定版本 → 接回应用
```

**新增的第 3 步很重要**：修改评测契约后，
旧的基线分数就不可比了（span 算法变了）。
必须用新契约把 P0 重跑一次，否则 P1/P2 没有正确的对照锚点。

---

## 六、明确认可的部分

- **优先级表（第六节）完全正确**，尤其"暂不处理检索质量"这一条，
  体现了一致的冻结纪律。
- **"先修评测、不要先改提示词"**：这是整份计划最有价值的一句。
  在指标不可信时优化提示词，等于用错误的尺子量东西。
- **raw / normalized 双留存**：完全正确，符合 trace 设计原则。
- **提示词固定变量清单**（同一 snapshot、同一 golden、同一模型、
  同一 max_evidence、同一参数）列得很严谨。
- **GitHub 项目的对应关系梳理**：`ragas` 对应指标分离、
  `Opik` 对应 trace 元数据、`production-rag` 对应独立指标——
  引用准确，没有生搬硬套。

---

## 七、一句话总结

**可以按这个方向推进，不需要改检索，也不需要重建快照。**

执行前请补三件事：
(1) 先把 `max_retries` 参数化，否则 9 次运行的延迟不可比；
(2) span 匹配的 bug 根因是**归一化后残留了 `（T1）` 的 `t1` 标签字符**，
同时破坏了 `座深`→`座深t` 和 `340`→`1340` 两个 token；
修法是**在归一化前**加一层括号/引用预清洗（已实测：q04 修复、其余零破坏），
不要改匹配器本体；
(3) 修完契约后**必须用新契约重跑一次 P0 作为锚点**，
否则 P1/P2 没有可比基线。

另外，`citation_status` 的分离建议从 span 扩展到 citation 类指标，
让 q12/q17 不污染事实题的引用统计。
