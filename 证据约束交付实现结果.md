# 证据约束交付实现结果

## 本轮已完成

- `GraphState` 增加 `deliverable`、`delivery_status`、`blocking_issues`、`allowed_citations`、`rewrite_attempts` 和 `evidence_policy`。
- 引用编号按本次证据集合重新计算；答案引用不存在的 `[Lx]`/`[Wx]` 时触发 `citation_invalid`。
- 间接来源、范围不匹配、未支持主张和网络摘要会进入阻断清单，不能被标记为可交付事实。
- 推荐必须包含适用条件、风险/限制、验证要求和有效引用；否则触发 `recommendation_unconditional`。
- 图流程变为：生成 → 审计 → 最多一次严格重写 → 合格交付或拒答。拒答时隐藏原始答案，只显示证据不足、阻断原因和待补资料。
- 本地检索增加进程内缓存和完整检索临界区串行化，避免多个规划子任务同时初始化/争用 BGE-M3；缓存键包含规范化查询、集合计数、top-k 和分块策略。
- `EMBEDDING_PRELOAD=true` 时启动阶段执行一次 embedding warmup，失败只记录为非阻断状态。
- 前端和执行指标显示 `deliverable`、`delivery_status`、阻断原因、允许引用和 direct/indirect 证据计数。

## 验证结果

在项目虚拟环境中执行：

```powershell
.venv\Scripts\python.exe -m pytest -q
```

结果：全部通过（121 tests）。仅有 Chroma/`asyncio.iscoroutinefunction` 的第三方弃用警告，不影响本轮行为。

新增契约测试覆盖：

- 不存在的 `[W8]` 引用必须阻断；
- indirect 来源不能形成可交付事实；
- 无条件推荐必须阻断；
- 允许引用集合从当前文档重新计算。

## 当前比较题的预期行为

PC/Tritan/玻璃比较题如果仍只有网络摘要或二手资料，应返回：

```text
状态：证据不足，已降级
可交付：否
阻断原因：citation_invalid / indirect_evidence / recommendation_unconditional
```

它可以作为“系统识别证据不足并拒绝不可靠结论”的演示案例，不能作为材料性能结论的成功案例。

## 尚未在本轮完成

- 官方网页/PDF全文抓取、PDF页码解析和来源白名单；
- PC、Tritan、玻璃官方 TDS/法规/标准资料的核验与重新入库；
- 在真实 Ollama 服务下重跑比较题、10题 R 评测并上传 LangSmith；
- GitHub 发布、Demo录制和截图整理。

这些属于下一阶段手动资料准备或重新评测工作，不能由当前间接搜索摘要替代。
