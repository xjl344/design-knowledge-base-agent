# 设计知识库助手 Demo 讲稿（2～3分钟）

## 演示问题

> 透明、可重复使用、需要洗碗机清洗的 500 mL 水杯，在 PC、Tritan 和玻璃之间如何筛选？

## 讲解顺序

### 0:00～0:20：项目定位

“这是一个面向设计决策的可审计 RAG Agent。它不只返回答案，还会显示答案使用了哪些资料、哪些内容是工程推导、哪些条件仍需验证。”

### 0:20～0:45：问题分析与规划

输入问题后，展示右侧执行追踪中的 `question_type`、`problem_spec` 和 `task_specs`。

说明系统把问题拆成材料合规、耐热/耐清洗、透明性、冲击性、重量和验证项目等子任务；独立子任务可以并行检索。

### 0:45～1:25：检索链路

展示执行追踪中的工具调用和来源列表。

说明检索顺序是：

```text
规则词典扩展 → BM25 + Dense 初召回 → RRF/重排 → 来源多样性控制
```

重点指出：标准和具体牌号 TDS 的证据等级不同，系统不会把“材料名称”直接当成合规结论。

### 1:25～2:00：证据审计与回答

展示回答中的：

- 资料事实；
- 工程推导；
- 设计假设；
- 推荐条件；
- 限制与待验证项。

可以这样解释：“如果 Tritan 的具体牌号有食品接触文件且洗碗机循环验证通过，它可能是轻量透明方案；但这不是仅凭材料名称得出的无条件结论。”

### 2:00～2:30：评测与边界

展示 R 策略10题基线：Recall@10 96.67%、MRR 93.33%、NDCG@10 84.32%。

强调：“这些是目标来源召回指标，不是答案正确率。k07 仍漏掉一个工艺指南来源，k10 的目标来源排名偏后，所以系统会保留限制并要求进一步核验。”

### 2:30～3:00：工程取舍

面试问答可用以下四点收尾：

1. R 分块在当前知识库和评测集上是选定的正式基线，但结果来自 R 分块与混合检索、重排和来源先验的组合。
2. BM25 负责标准号、材料牌号等精确词命中，Dense 负责语义召回，Cross-Encoder 负责候选重排。
3. 证据不足时系统会降级、补搜或明确拒绝过度推断，而不是编造精确值。
4. LLM、网络搜索或工具失败时，系统保留错误状态、重试/熔断信息和可追踪的执行轨迹。

## 录屏画面清单

- 主界面：问题输入、最终回答和参考来源。
- 执行追踪：问题类型、子任务、工具调用、节点耗时、证据矩阵。
- 评测摘要：R 策略总体指标和逐题边界结果。
- 不要展示：`.env` 内容、API key、模型权重路径中的个人信息、未经验证的“答案准确率”。

## 可能被追问的问题

**为什么不是只用向量检索？**

设计标准号、材料牌号和单位数值需要精确词匹配；BM25 与向量检索互补，重排器再处理候选排序。是否有收益用当前10题消融结果验证，而不是假设混合一定更好。

**为什么不直接给出最佳材料？**

材料选择依赖具体牌号、食品接触文件、温度、清洗条件、寿命和测试结果。系统输出带条件的建议，不把类别名称当成合规证明。

**10题是否证明系统已经生产可用？**

不能。10题证明当前检索基线可复现且来源召回较好；生产部署还需要更大评测集、答案级标注、性能压测、监控和真实用户反馈。
