# 设计知识库助手 Demo 讲稿

> **2026-09-25 重排。** 原版按 2～3 分钟写，与实测不符：**一道复杂题真实耗时 4～13 分钟**
> （规划拆 3 个子任务 → 3 次检索 × ~145 秒 + 生成，且常常还要一次严格重写）。
>
> **本版的做法：不假装它快，把等待时间当讲解时间用。**
> 主推那道题**冷检索 208 秒**，所以成片约 **5 分钟**——这个长度是**真实的**，
> 不是剪出来的。**不要为了压到 2 分钟去加速或跳剪检索过程**：
> 加速会让 4 分钟看起来像 20 秒，一旦被问「为什么这么快」就说不清。
> 画面上的计时器**不要隐藏**——真实耗时本身就是这个项目可信度的一部分。
>
> 如果面试只有 2 分钟：**放快版录屏（35 秒，缓存命中）**，然后口头说明
> 「这是缓存命中；冷检索是 208 秒，我有另一条录屏和全部 8 次运行的画像作证」。
> 这比硬塞一个剪辑版更可信。

---

## 一、选哪道题

| | 题目 | 实测耗时 | 适合 |
|---|---|---|---|
| **主推** | `GB/T 16252—2023 的名称和适用范围是什么？` | **冷检索 208.5 秒可完成，但三次里两次撞 300 秒上限；同进程再问 0.07~0.22 秒**（见下方说明）| 主片。能演示检索+审计全链路 |
| 备选 | `透明、可重复使用、需要洗碗机清洗的 500 mL 水杯，在 PC、Tritan 和玻璃之间如何筛选？` | **约 13.6 分钟**（815.7 秒） | 面试现场演示，或作为「复杂题」的补充片段 |
| 不建议 | 任何计算类问题（如容量估算） | **0.5 秒** | 快，但**完全不检索**（`sources=0`），演示不到 RAG |

### ⚠️ 同一个问题的耗时差三个数量级——根因是**进程内检索缓存**

2026-09-25 同一配置、同一道题连续八次运行。**八次都是** `question_type=simple`、
`sub_tasks=0`（不规划），`problem_spec` 逐字段相同（`intent=simple`、
`explicit_references=['GB/T 16252—2023']`）。差异全部由 `cache_hit` 解释
（`src/retriever.py` 的进程内 `_RETRIEVAL_CACHE`）。时刻是 checkpoint 落盘时间，
即该次运行的**结束**时刻：

| 运行结束 | `cache_hit` | 检索 | 重排 | 结果 |
|---|---:|---:|---:|---|
| 13:47:41 | **true** | 0.215 s | 136.451 s * | 可交付，2 条 `direct` |
| 14:00:18 | **false** | **300.047 s** | —（超时未记）| **降级拒答**，0 条来源 |
| 14:10:29 | **true** | 0.207 s | 179.995 s * | 可交付 |
| 14:15 / 14:17 / 14:19 | true | 0.163 / 0.070 / 0.068 s | 179.995 s * | 可交付 ← **本仓库截图与快版录屏** |
| 14:49:32 | **false** | **300.012 s** | —（超时未记）| **降级拒答**（该次与一个测试进程并发）|
| **15:10:58** | **false** | **208.491 s** | 177.281 s | **可交付，2 条 `direct`** ← **冷检索录屏** |

\* 命中时 `retrieval_profile` 保留的是**填充缓存那次冷检索**的组件耗时。
五次命中带着同一组 `重排 179.995 s / bm25 22.391 s` → 同一个缓存条目的五次命中。

所以：

- **冷检索**（该进程里第一次问这道题）：**实测 208.5 秒可以完成**；
  但**三次冷检索里有两次撞上 300 秒上限** → `retrieval_timeout` → 降级拒答
- **同进程内再问一次**：**0.07~0.22 秒**

> ⚠️ **不要把它说成「有时判成 simple、有时判成 complex」。** 09-24 的三次
> 226.6 / 254.6 / 297.7 秒曾被这样解释，但 09-25 八次全部是 `simple`，其中两次仍撞上限。
> **也不要笼统说「耗时不稳定」——现在知道它稳定地取决于缓存状态。**

**「一半对一半」这句话要主动说。** 冷检索能不能在 300 秒内跑完，本机实测三次里成功一次。
这是**已知边界**，不是待修 bug：300 秒上限是保护措施——它让超时那次**没有硬答**，
而是降级并说明原因。放开它属于「放宽上限」而不是「看得更深」，得先量出它原本在防什么。
面试时把这条主动讲出来，比被问出来强。

证据可复核：`python scripts/dump_retrieval_profiles.py` →
`docs/portfolio/retrieval_cache_evidence.json`（8 次运行的画像，5 次命中 3 次冷检索）。

所以录屏时：

- **想演真实冷检索**：重启应用（清空缓存）后立刻录，**留 3~5 分钟**。
  本仓库的 `demo_recording_cold.mp4`（4 分 11 秒）就是这么录的。
- **想演完整链路但控制时长**：先空跑一遍把缓存热起来，再录第二遍（十几秒）。
  本仓库的 `demo_recording.mp4`（35 秒）是这种。**两种都要在演示时说清是哪种。**
- 别在演示前刚重启又期待它快——那是必然的冷检索。
- **录制时不要同时跑测试或别的东西**：14:49 那次撞上限就与一个测试进程并发有关，
  负载会让重排变慢，录出来的耗时就不是干净数字。

⚠️ 另外：延迟受本机负载影响很大，同一条配置实测过 **26.0 / 84.5 / 161.7 秒**三种每题均值。

---

## 二、时间轴（真实时间，不剪等待；主推题成片约 5 分钟）

### 0:00～0:20 项目定位

> “这是一个面向设计决策的**可审计** RAG Agent。它不只返回答案，还会显示答案用了哪些资料、
> 哪些是工程推导、哪些条件仍需验证。我现在用它查一个国标的适用范围。”

**画面**：主界面。先亮出输入框。

### 0:20～0:35 输入问题并提交

输入 `GB/T 16252—2023 的名称和适用范围是什么？`，回车。

> “系统会先判断这是简单问题还是复杂问题——如果是复杂问题，它会先拆成子任务再并行检索。”

**画面**：问题出现在对话框，右侧「本次执行追踪」开始有内容。

### 0:35～4:05（真实 **约 3 分半**，冷检索；缓存命中则十几秒）**等待期间讲这些**

**这是全片最重要的部分——不要剪掉等待，讲满它。** 按下面顺序讲，每段对应屏幕上正在发生的事。
**如果这一遍是缓存命中（十几秒），只讲第 1、3、4 段，把 2 压成一句。**

1. **规划**（约 20 秒）
   > “它先判断这是简单问题还是复杂问题。如果是复杂问题，会先拆成子任务再并行检索——
   > 右边追踪面板里的 `question_type` 和 `task_specs` 就是这一步的结果。
   > **这道题八次运行全都是 `simple`**，所以待会儿那个几分钟的耗时**不是**分类造成的，
   > 是缓存状态造成的——第 4 段讲。”

2. **检索链路**（约 40 秒）
   > “检索顺序是：规则词典扩展 → BM25 + Dense 初召回 → RRF 融合 → Cross-Encoder 重排 →
   > 来源多样性控制。BM25 负责标准号、材料牌号这类精确词，Dense 负责语义召回，
   > 重排器负责把真正相关的候选提到前面。”

3. **证据等级**（约 40 秒）
   > “注意每份资料都带一个证据状态：`direct` 或 `indirect`。
   > **这个状态不是装饰**——交付闸门只允许用 `direct` 资料支撑【资料事实】。
   > 我踩过一个坑：问题写 `GB/T 16252—2023`，而文件名是 `GBT+16252-2023.pdf`，
   > 标点没归一化导致匹配失败，整本标准被判成 `indirect`，系统就拒答了。
   > 修好之后同一道题才通得过。”

4. **耗时**（约 20 秒）
   > “检索在这台机器上要几分钟——右边计时器显示的是真实数字，`retrieve` 节点 208 秒。
   > 这个延迟主要来自 Cross-Encoder 重排（177 秒），不是向量检索（1.8 秒）。
   > **这是冷检索**：这个进程里第一次问这道题。同进程里再问一次是 0.1 秒级——
   > 因为 `src/retriever.py` 有一个进程内缓存，命中就直接克隆返回、不跑重排。
   > 所以对外我不会说「这套系统 X 秒能答一道题」。”

5. **如果它拒答**（备用，约 30 秒）
   > “如果证据不够，它**不会**硬答。系统会隐藏没通过审计的原始答案，返回降级说明和阻断原因。
   > 这是设计意图，不是故障。”

### 3:10～3:40 答案与审计

**画面**：回答出现，右侧「参考来源」列出证据清单。

> “答案分成四类：【资料事实】带引用、【工程推导】说明假设、【设计建议】标明可信程度、
> 以及【待验证项】。这里每个【资料事实】后面都跟着 `[L2]`，指向右边来源清单里的那一条。”

### 4:35～5:05 评测与边界

**画面**：切到 `logs/portfolio_baseline/R_baseline_20260924_190441.md`。

> “这是 R 策略 10 题基线：Recall@10 **96.67%**、MRR **93.33%**、NDCG@10 **84.32%**。
> **这些是目标来源召回指标，不是答案正确率。**
> 已知边界也保留着：k07 缺一个 Tritan 工艺指南来源，k10 的目标来源排名第 4。
> 延迟我没有放进这张表——同一条配置三次运行是 26.0 / 84.5 / 161.7 秒，
> 它取决于机器负载，写成单值会误导。”

### 5:05～5:25 工程取舍收尾

四条，各一句：

1. R 分块是选定的正式基线，但结果来自 **R 分块 + 混合检索 + 重排 + 来源先验的组合**。
2. 证据不足时系统会降级、补搜或明确拒答，而不是编造精确值。
3. 交付闸门是**全有或全无**的：任何一条主张缺证据，整篇答案就被换成拒答。
4. LLM / 网络搜索 / 工具失败时，系统保留错误状态与可追踪的执行轨迹。

---

## 三、录屏操作步骤

**已有两条真实录屏**，静音、未加速未剪辑：

| 文件 | 时长 | 内容 |
|---|---:|---|
| `docs/portfolio/demo_recording.mp4` | 35 秒 | 缓存命中（快版，主片用）|
| `docs/portfolio/demo_recording_cold.mp4` | 4 分 11 秒 | 冷检索（慢版，用来证明真实耗时）|

要自己重录（脚本会驱动真实界面、提交问题、等到出现交付判定才截图，并录下全过程）：

```bash
# 1. 启动应用（ASCII 路径的 venv，中文路径的 .venv 启动不了）
cd /e/设计知识库助手
PYTHONUTF8=1 E:/venvs/design-kb-round2/Scripts/python.exe -u app.py
# 打开 http://127.0.0.1:7860

# 2. 另一个终端：录快版（先空跑一遍把缓存热起来，再录）
python scripts/capture_portfolio_assets.py --viewport-height 760 --mp4

# 3. 录冷版：先重启应用清空进程内缓存，然后立刻录，留 3~5 分钟
python scripts/capture_portfolio_assets.py --timeout 540 --viewport-height 760 --mp4

#    --mp4          额外转一份静音 H.264 MP4（需要 pip install imageio-ffmpeg）
#    --timeout      等待交付判定的上限秒数
#    --abort-after  超过这么多秒还没交付就放弃，用于重试慢运行
#    ⚠️ 放弃会取消服务端任务，但不会立刻释放 CPU——重试前先确认进程空闲
#       可用 python scripts/wait_for_idle.py --pid <应用PID> 等它闲下来
```

⚠️ **录冷版时不要同时跑测试或做别的事。** 14:49 那次冷检索撞上 300 秒上限，
就与一个测试进程并发有关——负载会把重排拖慢，录出来的耗时就不是干净数字。

脚本会用本机 Edge 无头运行、提交问题、等到出现交付判定（`可交付：是/否`）才截图，
并录下全过程。它**不会加速或剪辑**。

**录制前**：

- 先空跑一遍目标问题，**记下真实耗时**，据此安排讲解长度
- 确认 `.env` 里这三项没被改回去（否则会超时或慢 3 倍）：
  `LOCAL_RETRIEVAL_TIMEOUT_SECONDS=300`、`TOTAL_PIPELINE_TIMEOUT_SECONDS=600`、
  `QUERY_DECOMPOSE_ENABLED=false`

**录制中**：

- 画面**保留**计时器与耗时数字
- 等待期间**不要静音**，按上面第 2 节的 5 段讲
- 如果出现拒答，**保留它并解释**——这是加分项，不是失误

**录制后**：

- 只需掐掉「输入问题前的准备」和「最后的关闭窗口」，**不要加速检索过程**
  （加速会让 4 分钟看起来像 20 秒，一旦被问「为什么这么快」就说不清）

---

## 四、四张截图

已随仓库分发（`docs/portfolio/`），**都由脚本驱动真实界面产生，不是手工摆拍**：

| 文件 | 内容 |
|---|---|
| `screenshot_1_main_interface.png` | 问题输入框 + 最终回答（含 `[L1]`/`[L2]`）+ 参考来源清单 |
| `screenshot_2b_answer_tail.png` | 同一答案的尾部（证据不足与冲突 / 风险与验证 / 参考资料）|
| `screenshot_2_execution_trace.png` | 追踪面板（**缓存命中**那次）：`retrieve` 只用 0.2 秒 |
| `screenshot_4_cold_execution_trace.png` | 追踪面板（**冷检索**那次）：`retrieve` 208.5 秒，仍交付 2 条 `direct` |
| `screenshot_3_eval_baseline.png` | `R_baseline_20260924_190441.md` 的指标表 + 已知边界段 |

`2` 和 `4` 并排看就是「耗时差三个数量级」的现场——**同一道题、同一个面板、同一个字段**。

生成方式（可重跑）：

```bash
# 应用需先在 127.0.0.1:7860 运行
python scripts/capture_portfolio_assets.py --viewport-height 760 --mp4   # 截图 + 录屏
python scripts/capture_portfolio_assets.py --report-md logs/portfolio_baseline/R_baseline_20260924_190441.md
```

⚠️ 追踪面板**底部那个 JSON 是可滚动的**，一屏只显示前十几行（`session_id`、`status`）。
所以面板顶部加了一行**紧凑摘要**（问题类型 / 路由 / 子任务数 / 工具调用数 / 重写次数 /
节点总耗时 / 最慢节点 / 证据计数 / 主张支持率）——截图和一眼扫读都靠它，
原始 JSON 仍然完整保留在下面。

⚠️ 截图里聊天区会**自动滚到底部**，答案开头会被滚出视野；
脚本在截图前把聊天区滚回顶部，并额外存一张尾部的图。

⚠️ 追踪面板**不显示 `cache_hit`**。所以只看截图分不出这次是冷是热，要靠
`docs/portfolio/retrieval_cache_evidence.json` 里的画像对照。

---

## 五、不要展示

- `.env` 内容、API key、模型权重路径中的个人信息
- 未经验证的「答案准确率」——只能说**来源召回**
- 不要为了好看而隐藏拒答、隐藏耗时、或只挑一次成功的运行

---

## 六、可能被追问的问题

**为什么不是只用向量检索？**

标准号、材料牌号和单位数值需要精确词匹配；BM25 与向量检索互补，重排器再处理候选排序。
是否有收益用当前 10 题消融结果验证，而不是假设混合一定更好。

**为什么检索要几分钟？**

延迟主要来自 Cross-Encoder 重排（本机无 GPU 加速）——**冷检索实测 208.5 秒，其中重排占 177 秒**，
BM25 10.8 秒、向量检索 1.8 秒。
同一条配置三次运行的每题均值是 26.0 / 84.5 / 161.7 秒，说明它还高度依赖机器负载。
**向量检索本身不是瓶颈**——这也是当初给 Chroma 选 brute-force buffer 的理由。

（注意：这两句话只在**冷检索**时成立。缓存命中时整条检索是 0.1 秒级，见下一条。）

**同一个问题为什么两次耗时差三个数量级？**

**根因已定位：进程内检索缓存。** `src/retriever.py` 有一个进程级的 `_RETRIEVAL_CACHE`，
键是 `(question, top_k)`。命中就直接克隆返回，不跑 dense/rerank——**0.07~0.22 秒**；
不命中才走完整路径，**实测 208.5 秒可完成**（重排 177 秒 + BM25 10.8 秒 + 向量 1.8 秒），
而且**有时会超过 300 秒上限**，那次就是 `retrieval_timeout` → 降级拒答。

怎么定位到的：`retrieval_profile` 里同时有 `cache_hit` 和组件耗时，
而**命中时保留的是填充缓存那次冷检索的耗时**——五次命中带着完全相同的
`重排 179.995 s / bm25 22.391 s`，一眼就能看出是同一个缓存条目的五次命中。
全部 8 次运行的画像已导出成 `docs/portfolio/retrieval_cache_evidence.json`，
用 `python scripts/dump_retrieval_profiles.py` 可重跑。

**我先把这条写成「根因未定位」**，后来才查到缓存这一层。
我更愿意留一个诚实的未知，也不愿意编一个听起来合理的解释——
但查清之后就该把结论改过来。

**冷检索成功率高吗？**

**不高——三次冷检索里两次撞上 300 秒上限。** 这一条要主动讲，不要等被问。
300 秒上限是**保护措施**：它让超时那次没有硬答，而是降级并说明原因。
放开它属于「放宽上限」而不是「看得更深」，得先量出它原本在防什么——
所以这一版我**没有**调它，只把它记成已知边界。

**那系统还能用吗？** 能，但要知道边界：冷检索撞上限那次，系统**没有硬答**，
返回降级说明（0 条来源、`deliverable=false`），原始记录留在
`docs/portfolio/evidence_degraded_run_20260925_135405.json` 和
`docs/portfolio/evidence_cold_timeout_20260925_144324.json`。
宁可拒答、不可编造，这条在失败时也成立。

**顺带一个工程含义**：这个缓存是**进程内**的，重启就没了。
所以「这套系统多久能答一道题」这句话本身不成立——**它取决于这道题在这个进程里被问过没有**。
生产化要做的第一件事是把这一层换成可观测、可失效的缓存（带 TTL 与命中率指标），
而不是继续用进程内 dict 兜着。

**系统怎么保证不编造？**

三层：① 检索给每份资料打 `direct` / `indirect` 证据状态；
② 生成要求每条【资料事实】紧跟 `[Lx]` 引用；
③ 交付闸门逐条审计，**任何一条实质主张缺有效引用就隐藏整篇答案、返回拒答**。
第三层是全有或全无的，很严——长答案更容易触发，这是刻意的保守取舍。

**这套评测里最硬的一点是什么？**

**知道自己测不出来什么。** 提示词 A/B 在 15 个配对单元上给出 +0.093、CI95 [−0.023, +0.208]
——跨过 0。我没有把它写成「有效」，而是算出了 MDE = 0.105，并进一步证明
**加入效应为 0 的单元在数学上永远不能让 CI 不含 0**（上限 `n < (m·d/(1.96σ))²`，实测 9.6，
而现有 n=15 已超过它）。所以结论是「有界」而不是「有/无」。

**为什么它会拒答？**

交付闸门要求每条实质主张都有可定位的有效引用，且**任何一条不过就隐藏整篇答案**。
这是刻意的保守取舍：宁可拒答，不可编造。代价是长答案更容易触发拒答。

**10 题是否证明系统已经生产可用？**

不能。10 题证明当前检索基线**可复现**且来源召回较好；生产部署还需要更大评测集、
答案级标注、性能压测、监控和真实用户反馈。
