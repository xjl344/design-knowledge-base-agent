# 第二轮真实问题测试清单

## 先回答你的两个问题

### 不补充官方资料，能不能继续做？

可以，但展示定位必须写清楚：

- 可以展示系统原本的检索能力、混合检索、规划、引用审计、超时降级和拒答能力。
- 可以把 PC/Tritan/玻璃题作为“证据不足识别”案例。
- 不能把当前网络摘要支持的材料参数、BPA、耐温和寿命结论当作成功答案。
- 不能声称系统已经完成材料选型的可靠决策支持。

如果作品集目标是展示 Agent 工程能力，暂时不补资料也可以交付；如果目标是展示“设计决策答案质量”，至少要补一小组官方 TDS/法规/标准资料。

## 需要补充的内容

### 必须补充（即使不补官方资料也要做）

- 第二轮问题集和运行时间记录。
- 每题的 `status`、`deliverable`、`blocking_issues`、引用有效性和答案截图/JSON。
- 至少一题正常可交付、一题计算推导、一题条件化推荐、一题正确拒答、一题工具失败降级。
- 记录当前模型、分块策略、索引版本、是否启用网络搜索和运行耗时。
- README 中区分“检索基线指标”和“端到端答案指标”。

### 建议补充（为了证明答案质量）

- PC、Tritan、玻璃各一份官方 TDS 或官方产品资料。
- 至少一份适用的食品接触法规/标准原文。
- 每份资料记录版本、发布日期、页码/段落、适用牌号和测试条件。
- 重新 ingest 后，至少让 3～5 题能够由 direct 证据支撑。

### 可以暂时不补

- 全部35题都补齐官方证据。
- PDF自动抓取和来源白名单。
- 分布式检索服务。
- 知识图谱和多用户部署。

## 第二轮测试题

题目已经固化在 `data/test_qa_round2.json`，共8题，覆盖事实问答、计算推导、比较推荐、制造排障、标准边界、证据不足拒答、无效引用和端到端方案。

## 建议测试顺序

### 唯一运行方式：直接上传8题

第二轮不提供本地-only模式，直接运行8题并上传 LangSmith：

```powershell
.\scripts\Run-Round2Questions.ps1 -PythonPath 'E:\venvs\design-kb-round2\Scripts\python.exe'
```

脚本默认使用新建的 ASCII 路径环境：

```powershell
.\scripts\Run-Round2Questions.ps1 -PythonPath 'E:\venvs\design-kb-round2\Scripts\python.exe'
```

若需要额外使用 LLM 评测器：

```powershell
.\scripts\Run-Round2Questions.ps1 -PythonPath 'E:\venvs\design-kb-round2\Scripts\python.exe' -WithJudge
```

默认关闭网络搜索；如要观察网络补证路径，再运行：

```powershell
.\scripts\Run-Round2Questions.ps1 -WithWeb
```

逐题记录以下字段：

| 字段 | 记录内容 |
|---|---|
| 问题类型 | 事实/计算/推荐/拒答/故障等 |
| 是否完成规划 | 是否拆分任务，任务是否去重 |
| 证据情况 | direct / semi_direct / indirect / none |
| 引用 | 是否全部来自允许集合 |
| `deliverable` | 是/否 |
| `delivery_status` | completed / degraded_insufficient_evidence |
| 阻断原因 | citation_invalid、indirect_evidence 等 |
| 答案质量 | 0～2：错误、部分、符合预期 |
| 延迟 | 秒 |

### LangSmith检查

每次运行都会创建/同步 Dataset `design-knowledge-qa-round2-8`，并上传新的 Experiment；不是本地-only测试。上传的不是只有答案文本，还要检查 Experiment 结果和运行输出中是否有：

```text
deliverable
delivery_status
blocking_issues
allowed_citations
rewrite_attempts
direct_evidence_count
indirect_evidence_count
```

本地 `logs/round2_runs/<时间戳>.json` 与 LangSmith 使用同一组逐题字段；不要只看上传成功或总体分数。重点比较 `deterministic_scores`、`evidence_matrix`、`blocking_issues` 和三段延迟。

`expected_mode` 已按规则解析：必须拒答、条件化交付/降级、计算题和普通可交付题分别评分。`若缺官方牌号资料则拒答；资料完整时条件化推荐` 不再被当成无条件拒答。

## 通过标准

### 当前资料不补齐时

- 8题全部能完成流程，不因单题失败拖垮整批。
- 引用攻击题必须 `deliverable=false`。
- 证据不足题必须 `deliverable=false`。
- 工具故障题必须显示降级状态，不能显示 `completed`。
- 计算题必须公开公式、单位和假设。
- 推荐题不能输出无条件材料排名。

### 补齐一小组官方资料后

- 至少3题达到 `deliverable=true`。
- direct 证据支持的关键 claim 不低于80%。
- 无效引用率为0。
- 无条件推荐率为0。
- 拒答/降级识别率为100%。
- 端到端平均延迟单独报告，不与26秒检索基线混为一谈。

## 作品集应该怎么展示

建议展示两张结果表：

1. **检索优化表**：Recall@10、MRR、NDCG、延迟、k07/k10边界问题。
2. **真实问题表**：8题类型、deliverable、阻断原因、引用有效性、答案质量和延迟。

这能说明你前一轮优化的是“找得到来源”，这一轮验证的是“系统是否敢在证据不足时不交付”。两轮不能合并成一个准确率数字。

## 推荐结论

你现在应该进行第二轮测试。即使不补资料，也值得测试，因为它能验证系统的降级和拒答机制；但这轮的成功标准不是“所有题都答出结论”，而是“该交付时交付，该拒答时拒答”。
