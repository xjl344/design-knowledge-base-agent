# 设计知识库助手：作品集 v1.0 交付计划

## 目标与交付边界

目标是把当前项目整理成一个可以投递 AI Agent / RAG 实习的 GitHub 作品集。

本阶段只完成以下四件事：

1. 项目能够在 ASCII 路径下稳定安装、启动和测试。
2. README 能让陌生人理解项目、运行项目并复现实验。
3. R 策略的 10 题评测结果和已知限制被真实、清楚地呈现。
4. 具备一个 2～3 分钟的复杂问题演示素材。

本阶段不做：35 题困难问题增强、知识图谱、分布式部署、多用户鉴权、人工审核队列和新的 Agent 工具。

## A. 我可以直接完成的工作

当前执行进度：A1～A5 已完成。检查结果见 `作品集A5检查结果.md`；其中 Python 运行验证被中文工作区中的旧 `.venv` 启动失败阻塞，需要按 B1 手动迁移环境。

### A1. 整理项目状态与作品集叙事

- 将 `规划书新.md` 中已经实现的能力改成真实状态，删除或标记过时的“待实现”描述。
- 在 `README.md` 首页补齐项目定位：设计决策场景、CRAG 流程、R 策略、证据审计和故障降级。
- 明确项目定位为“可审计的设计知识库 CRAG Agent 原型”，不宣称已经是通用生产系统。
- 增加一张简洁架构图或 Mermaid 图，展示：问题分析 → 任务规划 → 混合检索 → 重排 → 证据审计 → 生成/网络补证。

验收标准：陌生读者只看 README 前半部分，就能知道项目解决的问题、主要技术点和运行入口。

### A2. 固化 R 策略评测证据

- 以 `logs/retrieval_ablations/final_R.json` 为当前主基线。
- 在 README 中记录以下指标及评测口径：
  - `Recall@10 = 96.67%`
  - `MRR = 93.33%`
  - `NDCG@10 = 84.32%`
  - 重排成功率 `100%`
  - 平均检索延迟约 `26 秒`
- 增加逐题结果表，至少标出 k07 的缺失来源和 k10 的首个命中排名偏后。
- 明确这些是“来源召回指标”，不是最终答案正确率。
- 说明最终结果来自“R 分块 + 当前混合检索/实体保护/权威性先验配置”的组合，而不是 R 分块单独贡献。

验收标准：读者可以从 README 追溯到原始 JSON，并能看懂指标含义、成功项和失败项。

### A3. 增加一键检查与评测入口

- 提供一个 PowerShell 检查脚本，检查 Python、依赖、模型路径、`.env` 关键变量和 Chroma 数据库是否存在。
- 提供一个 PowerShell 评测脚本入口，运行 R 策略10题检索评测并把输出保存到 `logs`。
- 脚本失败时输出明确的中文提示，不自动删除数据库、不覆盖用户文档。
- 在 README 中给出从进入虚拟环境到运行检查、启动界面的完整命令顺序。

验收标准：用户不需要阅读源码，就能按 README 完成检查、评测和启动；脚本对缺失模型、缺失密钥、数据库不存在等情况给出可行动错误。

### A4. 补齐作品集展示材料模板

- 在仓库中增加 Demo 讲稿模板，固定一个复杂问题，例如：
  `透明、可重复使用、需洗碗机清洗的500 mL水杯，在 PC、Tritan 和玻璃之间如何筛选？`
- 讲稿按“问题 → 规划 → 检索 → 证据审计 → 推荐条件 → 限制”组织。
- 增加面试讲解要点：为什么选 R、为什么需要 BM25 + Dense + Reranker、如何处理证据不足、如何降级。
- 增加项目截图/录屏所需的画面清单，但不伪造评测或性能结果。

验收标准：你可以按照讲稿在3分钟内完整演示一次，并能回答关键技术取舍。

### A5. 做本地可复现性检查

- 在当前工作区中检查启动入口、脚本路径、配置项和文档命令的一致性。
- 运行不依赖外部模型和网络的静态检查或单元测试；若当前中文路径导致虚拟环境启动失败，在文档中明确迁移方案。
- 不修改或删除用户已有的模型、知识库和日志。

验收标准：所有我能在当前环境安全完成的检查都有结果；无法完成的部分写明原因和你的手动步骤。

## B. 需要你手动完成的工作

### B1. 准备 ASCII 路径的虚拟环境

建议使用：

```powershell
py -3.14 -m venv E:\venvs\design-kb-round2
E:\venvs\design-kb-round2\Scripts\Activate.ps1
python -m pip install -r E:\设计知识库助手\requirements.txt
```

然后验证：

```powershell
python -m pytest -q
```

验收标准：测试命令能够启动；若有失败，保留完整错误输出，不要先跳过失败测试。

### B2. 配置本地模型和知识库

- 确认 `.env` 中的 `EMBEDDING_MODEL_PATH`、`RERANKER_MODEL_PATH` 指向实际模型目录。
- 确认 R 策略配置：`CHUNKING_STRATEGY=R`。
- 确认 `data\chroma_db` 已建立且来源文档存在。
- 启动本地 LLM 服务，并确认 OpenAI-compatible `BASE_URL`、模型名和 API key 配置正确。

验收标准：至少可以完成一个简单问题和一个复杂问题；答案中能够显示本地来源和执行追踪。

### B3. 运行并保存 R 策略基线

按 README 中的命令运行10题评测，确认结果与主基线口径一致。

你需要保存：

- 评测命令；
- 评测时间；
- 使用的 embedding/reranker/LLM 模型；
- 关键环境变量；
- 生成的 JSON 或 Markdown 报告。

验收标准：换一台机器或重新安装环境后，能够解释指标差异来自模型、索引或配置变化，而不是无来源的“截图分数”。

### B4. 录制 Demo 和准备截图

建议录制一个完整复杂问题，展示：

1. 用户输入问题。
2. Agent 判断为复杂任务并生成子任务。
3. 本地混合检索和重排。
4. 证据覆盖审计和来源等级。
5. 最终回答中的条件、风险和待验证项。

同时准备三张截图：主界面、执行追踪面板、评测结果表。

验收标准：视频控制在2～3分钟，不展示密钥、个人路径中的敏感信息或未经核验的性能结论。

### B5. 发布 GitHub 作品集

- 创建清晰的仓库描述和 Topics：`RAG`、`LangGraph`、`Agent`、`Information Retrieval`、`Python`。
- 检查 `.env`、缓存、模型权重和个人日志不会被提交。
- README 第一屏放项目定位、Demo 截图/视频链接和核心指标。
- 在项目说明中明确：网络搜索结果需要核验，系统不能替代正式法规认证或产品测试。
- 准备简历项目描述和面试讲解，不把“来源召回率”写成“答案准确率”。

验收标准：招聘者在1分钟内能看懂项目价值，在5分钟内能按 README 找到运行入口和评测证据。

## 推荐执行顺序

1. 我先完成 A1～A5，并提交文档、脚本和检查结果。
2. 你完成 B1～B2，确认本机环境和模型服务可运行。
3. 你完成 B3，重新生成一次 R 策略报告。
4. 我根据报告修正文档中的实际数字或错误命令。
5. 你完成 B4～B5，形成视频、截图和 GitHub 发布版本。

## 最终交付清单

> 状态更新于 2026-09-24（B3 收尾后）。

- [x] README 与实际代码状态一致。
      基线数字已改为引用脚本自己产出的报告；延迟已移出指标表并说明为何不可复现。
- [x] R 策略10题指标有原始文件和评测口径。
      `logs/portfolio_baseline/`（报告 + 原始 JSON）与 `logs/retrieval_ablations/final_R.json`
      已纳入仓库（`.gitignore` 加了精确例外）。
- [x] k07、k10 等边界问题被明确记录。
      改为由运行结果生成；k07 缺失来源已定位到
      `eastman_tritan_processing_mold_design_guidelines.pdf`。
- [x] ASCII 路径环境可以运行测试（`E:\venvs\design-kb-round2`，全量 538 passed / 1 既有失败）。
- [x] 项目可以启动并展示执行追踪。**✅ 2026-09-24 最终实测通过**（走通三个子系统的修复，见下）。
      `python app.py` 启动正常（`127.0.0.1:7860`，23 个组件含「本次执行追踪」面板），
      **并且提问能拿到答案**：GB/T 16252 那道题第 1 次生成被拒 → 严格重写一次 → 交付成功。
      ⚠️ 代价：**每道复杂题要 4~13 分钟**（规划拆 3 个子任务 → 3 次检索），
      且**常常要消耗一次严格重写**（即两次 LLM 调用）。录屏请留足时间或剪辑。
      <details><summary>2026-09-24 完整排查记录（三个子系统）</summary>
      `python app.py` 能启动（`127.0.0.1:7860`，HTTP 200，23 个组件含「本次执行追踪」面板），
      但**提问拿不到答案**：
      - 材料筛选题（讲稿那道）：815.7 s、3 个子任务、**降级拒答**
      - GB/T 标准分工题：659.9 s、3 个子任务、**降级拒答**
      - 容量计算题：0.5 s、干净答案（**但不检索**，sources=0）

      **根因（四个症状一个根因，已定位并修掉前四个）：**

      | # | 症状 | 状态 |
      |---|---|---|
      | 1 | 推荐义务逐句判定（要求同句同时有条件+风险+验证+引用）| ✅ 改为答案级 |
      | 2 | 表格表头被当成「无引用的推荐」| ✅ 改为结构判据（分隔行上一行）|
      | 3 | `claim.evidence_status` / `audit.warnings` 两条旁路 | ✅ 与 1 共用同一判定 |
      | 4 | **引用逐句生效，而模型把引用挂在段末** | ✅ 改为按段落作用域 |
      | 5 | **闸门全有或全无**：任一条阻断项隐藏整篇答案 | ❌ **待决策** |
      | 6 | 空节填充句 / 参考文献条目 / 标准编号被当成主张 | ✅ 已修（`669f618`）|

      第 4 条是根因：真实答案 30 条 claim 里 25 条 `cit=[]`，而正文有 39 行带 `[Lx]`。

      **修完第 6 条后，离线复算一份 792 字符、每行都带引用的答案，只剩一条阻断项**：
      `[unsupported] 本问题不需要工程计算或尺寸推导。`——「不需要」不在豁免词表里。

      ⚠️ **词表路线已被证明走不通**：三轮下来同一个语义每轮换个说法
      （`不涉及设计建议` → `未显示…` → `不需要…`）。补一个词能让这一次通过，
      下一次运行换个词又挂。

      → **加上第 5 条，一份 3000 字报告只要有一句话没引用，整篇就被换成拒答。**

      **两条结构性方案（未做，需决策）：**
      - **A（推荐）按章节豁免**：七段式提示词自己定义了 §五 证据不足与冲突 /
        §六 风险与验证 / §七 参考资料——这三节的存在意义就是陈述「不知道什么」
        和「还要做什么」。机械判据：跟踪 `## …` 标题。代价：模型可以把一条
        无依据的主张藏进 §六。
      - **B 按覆盖率阻断**：`deliverable` 改为「有引用且可解析的实质主张占比 ≥ 阈值」。
        项目里**已经有** `claim_support_rate` 这个量，只是闸门没用它。
        代价：阈值没有自然取值。

      **修好第 5 条之前，Demo 录出来会是一次拒答。**

      ---

      **2026-09-24 最终实测（实施「按章节豁免」之后）：阻断项从 4 条降到 1 条，但 App 仍拒答。**

      - 第 1 次生成（493 字）：只剩 1 条 `indirect_evidence`
      - 第 2 次不是重写成功，而是**重写调用被 provider 502 打断**（`Upstream access forbidden`）
        → 追加 `rewrite_failed`

      ⚠️ **而剩下这一条的根因不在闸门里。** 查这道题实际检回的 6 份文档，
      **全部是 `indirect`，包括被问的那本标准本身**：

      ```
      L1: status=indirect  source=03_…/GBT+16252-2023.pdf   ← 就是被问的标准
      L2: status=indirect  source=03_…/GBT+16252-2023.pdf
      ```

      答案引用 `[L2]`（标准正文），闸门按自己的规则说「仅由间接资料支持，不能形成可交付定案」
      → 拒答。**闸门是对的，是输入的标签错了。**

      标签来自 `src/retriever.py:324` → `classify_document_for_question`（`src/document_metadata.py:196`），
      由 `RETRIEVER_MIN_RELEVANCE`（默认 0.25）把关。

      → **因果链已经走过三个子系统**：① 检索预算（已修）② 交付闸门（已修 8 处）
      ③ **证据状态判定把权威标准判成 `indirect`（未查）**。

      **下一步：查 `classify_document_for_question` 为什么把标准正文判成 `indirect`
      ——这是第三个子系统，不要继续在闸门里修。**

      ---

      ## ✅ 2026-09-24 收尾：**App 现在能出答案了**

      第三个子系统的根因已定位并修复。**`src/document_metadata.py:247` 的引用匹配只归一化了空格**：

      ```
      问题写  GB/T 16252—2023
      文件名  GBT+16252-2023.pdf
      → GB/T ≠ GBT+、— ≠ -  → 匹配失败 → 落到兜底分支 indirect
      ```

      而 `retrieval_evidence_reason` 直接点名了分支（「资料与问题领域相关，但未形成直接支持」），
      打印它就能定位——**这比再跑一次 10 分钟的真实运行便宜得多**。

      **修法**：`_designation_key()` 比较前去掉所有非字母数字字符（保留 CJK）。
      **评测安全**：只影响实时检索的标签；冻结快照里记录的是当时算好的标签，排名与参数未动。

      **最终实测**：

      | | claim 数 | 可交付 | 阻断项 |
      |---|---|---|---|
      | 第 1 次生成（761 字）| 11 | False | 1 条：引导句「…生产与选用**：**」自己没有引用 |
      | 第 2 次（严格重写，439 字）| 3 | **True** | 无 |

      → **设计的机制在工作**：第一次不过 → 严格重写一次 → 通过 → 交付。
      → **交付清单里「项目可以启动并展示执行追踪」可以改回 ✅。**

      ### 今日为让 Demo 能答题，一共走过三个子系统

      | # | 子系统 | 缺陷 | 提交 |
      |---|---|---|---|
      | ① | 检索预算 | 20 s vs 实测 129/374 s，App 从未成功检索 | `4b32380` |
      | ② | 交付闸门 | 粒度 / 作用域 / 豁免，共 **8 处** | `8b4b32e`…`2c020e0` |
      | ③ | 证据状态判定 | 标准编号标点未归一化，权威标准被判 `indirect` | `7fdff61` |

      **全量测试 1 failed / 555 passed / 4 skipped**（唯一失败是既有的 `test_graph.py`）。

      </details>
- [ ] 有2～3分钟 Demo 和三张关键截图。**未做**（讲稿本身是完整的）。
- [ ] GitHub 仓库不包含密钥、模型权重和无关日志。**未做**：`.git/config` 里没有任何 remote，
      仓库尚未发布。密钥侧的静态检查已通过（`.env`、`.env.bak.timeout60` 均被忽略）。
- [x] 简历和面试表述严格区分检索指标与答案质量（README 与 `作品集Demo讲稿.md` 都写明）。

### 还差的步骤（按顺序）

> ✅ **2026-09-24 更新：原先插在第一步之前的两个阻塞已修完**（检索预算 + 三个子系统的证据链）。
> App 现在能答题。

0. ~~先修两个阻塞~~ **✅ 已完成**（提交 `4b32380` / `8b4b32e`…`2c020e0` / `7fdff61`）：
   - **交付闸门**：粒度 / 作用域 / 豁免共 8 处，全部改成结构判据
   - **检索预算**：`LOCAL_RETRIEVAL_TIMEOUT_SECONDS` 20 → 300、`TOTAL_PIPELINE` → 600、
     `QUERY_DECOMPOSE_ENABLED=false`
   - **证据状态**：标准编号标点未归一化，权威标准被判 `indirect` → 已修

   ⚠️ **仍未解决的是「时长」**：每道复杂题 4~13 分钟（规划拆 3 个子任务 → 3 次检索），
   且常常要消耗一次严格重写。**录屏请留足时间或剪辑**，不要试图压到 2 分钟。
1. 启动 `python app.py` 确认界面与「本次执行追踪」面板可用，并截三张图
   （主界面 / 执行追踪 / 评测结果表）。
2. 按 `作品集Demo讲稿.md` 录 2～3 分钟演示视频。
3. 建 GitHub 远端并推送；README 第一屏补上 Demo 链接与截图。
4. 发布后按 B5 清单复核一次仓库描述、Topics 与「无密钥/权重/无关日志」。

### 一处需要留意的边界

R 基线的**召回指标可复现**（三次运行里两次逐项完全相同），但**延迟不可复现**
（26.0 / 84.5 / 161.7 秒，同一条配置）。对外表述延迟时给区间并说明原因，
不要写成单值。

### ⚠️ Demo 的单题成本（2026-09-24 实测，修完超时配置后）

| 环节 | 耗时 |
|---|---|
| 检索（单查询路径） | **约 145 秒** |
| 生成 | 约 90 秒 |
| **合计** | **约 4 分钟** |

所以讲稿里写的「2～3 分钟」需要**选一个检索快的题**才压得住
（R 基线 10 题里最快的 67.5 秒）。演示前请先跑一遍计时，别在录屏时才发现超时。

**必须保留的配置**（`.env`，否则 Demo 跑不起来）：

- `LOCAL_RETRIEVAL_TIMEOUT_SECONDS=300`（原 20 秒 → 每次检索都超时）
- `TOTAL_PIPELINE_TIMEOUT_SECONDS=600`（原默认 120 → 会把上面的值判成死配置）
- `QUERY_DECOMPOSE_ENABLED=false`（多查询把检索从 129 秒拉到 374 秒；
  且 R 基线本来就是单查询路径测的，关掉后 App 与发布数字走同一条路）

**为什么留 Agent 链路、不换成评测用的严格证据链路**：Demo 讲稿的截图清单要求
「执行追踪：问题类型、子任务、工具调用、节点耗时、证据矩阵」——只有 Agent 链路会填这些；
而且作品集目标是 AI Agent / RAG 实习，严格证据链路是**一次检索 + 一次生成**，
只留它等于把「Agent」这个词从作品里删掉。评测链路留在评测侧，不进界面。

## 本阶段完成定义

当 README、评测证据、运行命令、Demo 和 GitHub 仓库都能被陌生人理解并复现时，v1.0 作品集交付完成。此后再决定是否进入 v1.1 困难问题增强，不在本阶段继续扩展功能。
