# 作品集 v1.0 A5 检查结果

检查时间：2026-09-13

执行命令：

```powershell
.\scripts\Check-PortfolioReadiness.ps1
```

## 结果

| 检查项 | 结果 |
|---|---|
| 关键项目文件 | PASS |
| `.env` 密钥保护 | PASS，`.env` 已在 `.gitignore` 中 |
| R 分块配置 | PASS，`CHUNKING_STRATEGY=R` |
| 正式 Chroma 索引 | PASS，`data\chroma_db\chroma.sqlite3` 存在 |
| BGE-M3 embedding | PASS，配置目录存在 |
| BGE reranker | PASS，配置目录存在 |
| PowerShell 脚本语法 | PASS，`Check-PortfolioReadiness.ps1` 和 `Run-RBaseline.ps1` 均可解析 |
| Python 可执行文件 | FAIL，当前中文工作区中的 `.venv` 无法启动 |

## 当前阻塞与处理方式

当前 `.venv\Scripts\python.exe` 在中文项目路径下启动时出现 Windows 路径乱码/进程创建失败，因此本机尚未运行 `python -m pytest -q`。新的 R 基线脚本也已实际尝试启动，但在调用该 Python 时以退出码 `101` 失败。该问题不属于业务代码失败，也不会修改或删除知识库数据。

请手动在 ASCII 路径重新创建环境：

```powershell
py -3.14 -m venv E:\venvs\design-kb-round2
E:\venvs\design-kb-round2\Scripts\Activate.ps1
python -m pip install -r E:\设计知识库助手\requirements.txt
python -m pytest -q
E:\设计知识库助手\scripts\Check-PortfolioReadiness.ps1 -PythonPath E:\venvs\design-kb\Scripts\python.exe
E:\设计知识库助手\scripts\Run-RBaseline.ps1 -PythonPath E:\venvs\design-kb\Scripts\python.exe -NoLangSmithUpload
```

## 已完成的静态验证

- 两个新增 PowerShell 脚本通过 PowerShell AST 语法解析。
- 检查脚本不会输出 API key、模型权重内容或完整 `.env`。
- R 基线脚本只临时设置 `CHUNKING_STRATEGY`、`CHROMA_DIR`、`COLLECTION_NAME` 和 `RETRIEVER_SOURCE_CAP`，退出后恢复原环境变量。
- 没有修改、删除或重建现有文档、Chroma 数据库和模型目录。
