# 冻结检索、单独重建生成链路 —— 可行性核查

核查日期：2026-09-19
核查对象：`冻结检索、单独重建生成链路` 计划书
核查方式：只读核对代码、评测日志、Chroma 索引与数据集，未修改任何项目文件

---

## 一、总体判断

**方向正确，但 Phase 0 的落地路径有一个致命的、必须改的前提。**

计划的核心判断——"检索冻结为已验证资产，只重建生成链路"——与项目实情相符，
文件的模块边界也支持这种做法。但计划指定的快照来源
`logs/retrieval_ablations/final_R.json` **无法直接还原成可复放的证据包**，
原因是该快照与现有 Chroma 索引已经代际错位。

如果按原计划直接开工，Phase 0 会在"回填正文"这一步卡住，
而 Phase 1 的 `frozen_evidence.py` 会建在空数据上。

---

## 二、致命问题：快照无法回填正文

### 2.1 实测结果

`final_R.json` 的 `ranked_chunks` 含有 50 个字段，
其中包含 `chunk_id`、`source`、`combined_rank`、`rerank_score`、`retrieval_evidence_status` 等，
**但不包含 `page_content`、`content_hash`、`page`**。

计划的 Phase 0 要求快照记录 `page_content` 与 `content_hash`，
这两个字段在 `final_R.json` 里根本不存在。

### 2.2 尝试用 chunk_id 回填，失败

设计上的回填路径是：用 `chunk_id` 去 Chroma 按 id 取回正文。

实测（对 10 题、共 92 个 chunk_id 全量尝试）：

```text
TOTAL 2 / 92
```

**92 个只有 2 个能取回。** 即 97.8% 的快照 chunk 无法定位正文。

### 2.3 根因：哈希方案一致，但索引已重建

先排除了"id 方案不一致"的假设。核对 `ingest.py:230 make_chunk_id`
与 `src/chunking.py:238`，两者身份串构造完全一致：

```text
source \x1f page \x1f chunk_strategy \x1f chunk_version \x1f start_index \x1f page_content
```

对当前 R 库抽样复算，db_id / meta.chunk_id / 复算值三者完全相等，
说明 id 方案本身是自洽的。

真正的原因是**时间线错位**：

| 对象 | 时间 |
|---|---|
| `logs/retrieval_ablations/final_R.json` | 2026-09-08 23:08 |
| `data/chunking_experiments/R/chroma.sqlite3` | 2026-09-19 16:27 |
| `data/chroma_db/chroma.sqlite3` | 2026-09-13 13:13 |

由于 `start_index` 参与了 `chunk_id` 的哈希，
索引一旦重建（重新分块、参数变动、文档增删），
**所有 chunk_id 会整体失效**，而不是局部失效。

快照是 9 月 8 日的，索引是 9 月 19 日的，中间至少经历了一次重建。
所以 92 个里只能活下 2 个偶然命中的。

### 2.4 结论

> `final_R.json` 可以作为**指标基线**（source_hit 96.67% 等仍然有效），
> 但**不能作为证据快照的来源**。

这两个用途必须分开。计划把"指标基线"和"证据快照"混为一谈了。

---

## 三、次要问题：其余需要修正的假设

### 3.1 超时归因基本正确，但数值要修正

计划说"评测 profile 会把本地检索超时覆盖到约 20 秒"。
核对后确认存在这个机制，但数值更微妙：

```text
config.py:143  local_retrieval_timeout_seconds = 180   （.env:35 覆盖值）
config.py:145  local_retrieval_total_timeout_seconds   默认回退 20
config.py:156  total_pipeline_timeout_seconds = 120
```

最近一次评测 `logs/round2_runs/20260918_143623.json` 的 runtime 显示：

```text
local_retrieval_timeout_seconds: 180.0
local_total_timeout_seconds: 20.0
total_pipeline_timeout_seconds: 120.0
```

结果是 8 题里 7 题 `degraded_infra_timeout`，
`timeout_stage = total_pipeline`，`retrieval_duration_seconds = 0`、
`generation_duration_seconds = 0`、`tool_calls = []`。

**生成模型一次都没被调用。** 这一点计划判断正确。

但更根本的矛盾是：单次检索允许 180s，整条管道只有 120s。
这个矛盾在生成重建之前必须先行修掉，否则新生成器接回去仍然会被掐死。
**这是运行层修复，不属于检索算法改动，不违反冻结约束。**

### 3.2 评测集编号要用 q01~q20 的口径

计划的主评测集写作 `q01~q10、q12、q17`。
核对 `data/test_qa_20.json`，实际 id 为：

```text
q01_hit ~ q11_hit      （可命中）
q12_miss ~ q16_miss    （本地无证据）
q17_ambiguous ~ q20_ambiguous
```

计划里的 `q12`（本地无证据拒答）和 `q17`（多来源综合）与数据集语义吻合，
但要注意 id 带后缀（`q12_miss`、`q17_ambiguous`），不是裸 `q12`/`q17`。
另外 `data/test_qa_round2.json` 是 `r2-01 ~ r2-08`，与 q01~q20 是两套题，
不要混用。

### 3.3 模型名需要确认

计划写"模型固定使用 DeepSeek-V4-Flash"。
但最近评测的 runtime 记录 `llm_model: gpt-5.6`。
如果确实要切到 DeepSeek-V4-Flash，需要在 `config.py` / `.env` 里显式改，
并确认 `LLM_BASE_URL` 兼容。这一点计划没有说明改哪里。

### 3.4 `evidence_status` 无法从快照还原

计划的快照字段含 `retrieval_evidence_status`。
该字段在 `final_R.json` 里存在，但它的计算依赖 `applicability`、
`population`、`material_grade` 等元数据，
而这些元数据在快照里也没有完整保留。
如果新证据包需要这个状态，必须一并从 Chroma 元数据回填。

---

## 四、修订后的可行路径

### 关键改动：Phase 0 改为"带正文的可复放快照"

不再从 `final_R.json` 回填，改为**重新跑一次受控检索来固化快照**。

这**不违反冻结约束**，因为：

- 不修改 `src/retriever.py`；
- 不改分块、Embedding、Reranker、Chroma；
- 不调 `RETRIEVER_*` 参数；
- 不重新比较 Dense/BM25/RRF；
- 只是**调用一次现有检索器，把输出原样落盘**，之后永久不再调用。

这与计划原本"快照来源"的意图一致，只是把来源从"旧的失效产物"
换成"当前索引的一次性读取"。

### 修订后的 Phase 0

1. 用一个一次性脚本，对 `data/test_qa_20.json` 的 q01~q10、q12、q17
   调用**现有检索器**（不改任何参数），把每个 chunk 的
   `page_content`、Chroma 原始 id、元数据（含 `applicability`、
   `population`、`material_grade`、`source_role`、`page`）
   一并写入 `data/frozen_retrieval_cases.jsonl`。
2. 同步记录 `retrieval_config_hash`、`collection_name`、
   `chunking_strategy`，用于声明快照来源。
3. **同时冻结索引**：快照生成后，记录 R 库的
   `chroma.sqlite3` 指纹（如 sha256 或 count+manifest 哈希）。
   只要索引不变，快照就永久可复放；索引一旦重建，
   Phase 0 必须重跑。这一点必须在文档里写死。
4. 运行结束后进行一次自检：**逐条断言快照里的
   `page_content` 非空**。任何一题为空，标记 `not_replayable`，
   按计划要求排除出生成质量评测。

这样 `frozen_evidence.py` 的输入才真正完整。

### 其余 Phase 不变

Phase 1（证据包构建器）、Phase 2（最小生成器）、Phase 3（软审计）、
Phase 4（开关接回）的设计是合理的，可以保留。它们不依赖旧快照。

### Phase 顺序上的一个建议

把"修超时矛盾"从隐含前提提为**显式的 Phase 0.5**。

在生成链路重建完成、准备接回应用之前，
必须先解决 `180s 单次检索 vs 120s 全管道` 的冲突。
建议取值：

```text
LOCAL_RETRIEVAL_TIMEOUT_SECONDS = 60
TOTAL_PIPELINE_TIMEOUT_SECONDS  = 180   （含生成 60s）
```

具体数值应在实测检索延迟（基线均值约 26s，冷启动更高）后确定。

---

## 五、验收标准补充

在原计划验收项之外，增加两条：

1. **快照完整性**：`data/frozen_retrieval_cases.jsonl` 中
   每个 chunk 的 `page_content` 非空率 = 100%；
   或明确列出 `not_replayable` 题目并排除。
2. **索引一致性**：记录并断言 `chroma.sqlite3` 指纹，
   确保整个生成实验期间索引未变动。

---

## 六、一句话总结

计划的方法论（冻结检索、离线复放生成、软审计先行）是对的，
可以执行；但 Phase 0 必须从"回填旧快照"改为"用当前索引
一次性生成带正文的新快照"，否则证据包的第一步就是空的。
